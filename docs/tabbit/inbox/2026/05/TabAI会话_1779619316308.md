### **以下是给 Trae 的"验收汇报"指令模板，按交付物、协议、SDK、admin 边界、测试、文档六大维度逐项核对，确保此前三轮裁决（迁移方案 → Schema/模板 → SDK + admin 边界）全部落地，并暴露任何偏差与残留风险。**

把下面这段整段发给 Trae 即可，它会按结构化模板回填证据。验收口径以 **可执行命令 + 文件路径 + 行号 + 实际输出** 为准，不接受"已完成"这类描述性表述。

---

```text
主控验收指令：全局配置与多语言迁移 + Schema/模板 + SDK + admin 边界 全量验收

请按以下模板逐项汇报，每项必须给出 文件路径、关键代码摘录、可执行命令、实际输出 四要素。
不接受"已完成 / 已对齐 / 已实现"等无证据描述。
不允许跳过任何一项，无内容的项必须明确写"未实施"或"不适用"并说明原因。

================================================================
# 全量验收报告：Config + I18n 迁移与 SDK 落地

## 0. 总体结论
- 状态：可发布 / 有阻断 / 部分通过
- 阻断项数量：N
- P0 残留风险数量：N
- 建议下一步：xxx

================================================================
## 1. 目录与分层验收

### 1.1 目录是否齐全
对照下列清单，逐项给出 `ls -la` 输出：
- go/services/config/{domain,repository,cache,service,sdk}/
- go/services/i18n/{domain,repository,cache,service,sdk}/
- go/tars/config/cmd/
- go/tars/i18n/cmd/
- go/tars/provider-admin/cmd/
- go/third_party/{mysqlx,redisx,sqlitex}/
- proto/base/app_config.proto
- proto/base/i18n.proto
- configs/routes.yaml

### 1.2 分层依赖是否合规
执行：
  go list -deps ./services/config/sdk/... | grep -E "(repository|service)"
  go list -deps ./services/i18n/sdk/... | grep -E "(repository|service)"
要求：SDK 不直接依赖 repository（必须经 service）。
请贴出实际输出。

### 1.3 单文件行数检查
执行：
  find go/services -name "*.go" -not -name "*_test.go" -exec wc -l {} \; | sort -rn | head -20
要求：所有文件 ≤200 行。超过的列出文件名 + 行数 + 拆分计划。

### 1.4 单函数行数检查
列出所有 ≥50 行的函数（文件:函数名:行数）。

================================================================
## 2. 协议层验收

### 2.1 协议编号注册表
贴出 docs/api/协议编号注册表.md 中 6001-6019 段落原文。
确认登记：6001 / 6003 / 6005 / 6007 / 6009 五条。

### 2.2 Protobuf 定义
- proto/base/app_config.proto
  - 是否包含 AppConfigsRsp.dynamic_modules（field number、类型）
  - 是否保留旧字段 base_cfg/wap_cfg/... 兼容
  - 是否定义 DynamicConfigModule、FieldDescriptor
- proto/base/i18n.proto
  - LangStringEntry 是否包含 template_type / params
  - 是否定义 LangParam
请贴出关键 message 定义原文。

### 2.3 routes.yaml
贴出 6001/6003/6005/6007/6009 五条路由原文。
确认 servant 命名符合 Tars 规范（CaiRobot.XxxServer.XxxObj）。

### 2.4 Tars IDL
- tars/protocol/tars/config.tars
- tars/protocol/tars/i18n.tars
贴出方法签名，确认全部使用 bytes 接口。

================================================================
## 3. 数据层验收

### 3.1 表结构
对下列表给出 `SHOW CREATE TABLE` 输出：
- sys_config_schema（新增）
- sys_config_version（保留 + 字段调整）
- sys_lang_pack
- sys_lang_string（template_type / params_schema / preview_sample 三字段）
- sys_lang_pack_release

### 3.2 旧字段处理
- sys_config_version 中 lang_pack_default / lang_pack_weba 当前是否仍在被读取
- 列出所有引用这两个 module_key 的 Go 文件路径与行号
要求：业务读路径 0 引用，仅迁移期镜像写入。

### 3.3 版本号归一化
- member_cfg.version 当前实际值（SELECT module_key, env, version FROM sys_config_version）
- 是否已从时间戳迁移为递增整数

### 3.4 索引
sys_lang_string 是否新增 (pack_id, version) 复合索引，贴出 SHOW INDEX 输出。

================================================================
## 4. services 层验收

### 4.1 services/config
列出以下文件并贴出对外接口签名：
- service/service.go
- service/fetch.go
- service/parse.go（按 schema 解析，禁止硬编码 module 字段）
- service/validate.go
- service/schema.go
- service/compose.go
要求：grep 不到 "member_cfg"/"base_cfg" 等硬编码 module_key 的 switch/if 链。
执行：
  grep -rn "member_cfg\|base_cfg\|pay_cfg" go/services/config/service/
贴出输出。

### 4.2 services/i18n
列出：
- service/service.go
- service/pack.go
- service/diff.go
- service/language.go
- service/publish.go
- service/template_validator.go
- service/compat_filter.go
要求：template_validator 必须强校验占位符与 params_schema 一致。
贴出 template_validator 核心函数实现。

### 4.3 缓存策略
执行：
  grep -rn "config:" go/services/config/cache/
  grep -rn "i18n:" go/services/i18n/cache/
列出所有 cache key 模板，确认：
- 不含硬编码 v3
- version 由 DB 驱动
贴出 cache key 生成函数实现。

================================================================
## 5. SDK 层验收（重点）

### 5.1 configsdk 接口
贴出 go/services/config/sdk/client.go 中 Client interface 完整定义。
确认包含：
- GetString / GetInt / GetBool / GetFloat / GetJSON
- GetModule / Bind
- Watch / Ping

### 5.2 i18nsdk 接口
贴出 go/services/i18n/sdk/client.go 中 Client interface 完整定义。
确认包含：T / Raw / BatchT / Watch / Ping

### 5.3 InProcess / Remote 双模式
- 列出 sdk.Init() / sdk.Options 结构体定义
- 贴出 InProcess 与 Remote 两条初始化分支代码
- 确认两种模式下业务调用代码完全一致（无 if Mode 分支）

### 5.4 三层缓存
- L1（进程 LRU）实现位置 + 容量 + TTL
- L2（Redis）key 命名 + TTL
- L3（远程/同进程 service）兜底逻辑
贴出三层 fallback 的核心代码。

### 5.5 Watch + pub/sub 失效
- 订阅 channel 名称：cairobot.config.invalidate / cairobot.i18n.invalidate
- payload schema 定义位置
- SDK 收到 pub/sub 后清 L1 + 触发 Watch 回调的代码
- 失效延迟实测（ms 级）

### 5.6 SDK 使用约束验证
执行：
  grep -rn "services/config/service\|services/config/repository" go/services/openapi/ go/services/devicegw/ go/services/usercenter/ 2>/dev/null
要求：业务服务 0 直接 import services/config 内部包，必须只通过 sdk。
贴出输出（应为空）。

================================================================
## 6. admin-server 边界验收（铁律）

### 6.1 admin-server 职责清单
确认 admin-server 仅暴露以下能力，列出所有路由（gin route 列表）：
- /api/admin/config/schema
- /api/admin/config/value
- /api/admin/i18n/string
- /api/admin/i18n/pack
要求：不存在任何 /api/business/* 或 /api/openapi/* 路由。

### 6.2 admin-server 写入后失效
贴出 admin-server 中保存配置/i18n 后的：
- Redis key 失效代码
- pub/sub publish 代码
- 校验顺序：先校验 schema/template → 再写 MySQL → 再失效缓存 → 最后 publish

### 6.3 业务服务 0 调用 admin-server
执行：
  grep -rn "admin-server\|provider-admin" go/services/openapi/ go/services/devicegw/ go/services/usercenter/ go/services/aiservice/ 2>/dev/null
  grep -rn "8081\|admin.cairobot" go/services/ 2>/dev/null
要求：输出为空。

### 6.4 网络段隔离
贴出部署文档中 admin-server 的网络段说明（应仅运维内网可达）。

================================================================
## 7. 快速增加配置 / 多语言闭环演示

### 7.1 配置闭环演示（端到端）
请实际执行以下流程并贴出每一步证据：
1. 通过 admin-server API 新增 sys_config_schema 一行：
   module_key=demo_cfg, field_key=test_int, type=int, default=42
2. 立即在测试业务服务中调用：
   configsdk.Default().GetInt(ctx, "demo_cfg", "test_int")
3. 期望返回 42
4. 通过 admin-server API 修改值为 99
5. SDK 在 N 毫秒内返回 99（贴出 N 实测值）

### 7.2 多语言闭环演示（端到端）
1. 通过 admin-server API 新增 string_key=demo_msg：
   template_type=named
   params_schema=[{name,string},{count,int}]
   zh-CN: "你好 {name}，共 {count} 项"
   en:    "Hi {name}, {count} items"
2. 业务服务调用：
   i18nsdk.Default().T(ctx, "zh-CN", "demo_msg", {"name":"张三","count":3})
3. 期望返回 "你好 张三，共 3 项"
4. 客户端通过 6007 协议拉到的 LangStringEntry 是否包含 template_type 与 params

### 7.3 客户端版本兼容
- 老客户端（client_version 低于阈值）调用 6005，是否被 compat_filter 过滤掉 named/icu key
- 贴出过滤函数实现 + 一条端到端测试输出

================================================================
## 8. 测试验收

### 8.1 单元测试（SQLite）
执行：
  cd go && go test ./services/config/... ./services/i18n/... -count=1 -v 2>&1 | tail -50
贴出输出（PASS/FAIL 数量）。
覆盖率：
  go test ./services/config/... -cover
  go test ./services/i18n/... -cover
要求：≥80%。

### 8.2 集成测试（MySQL）
执行：
  cd go && go test -tags=integration ./services/config/... ./services/i18n/... -v 2>&1 | tail -50
贴出输出。

### 8.3 SDK 测试（含 miniredis）
执行：
  cd go && go test ./services/config/sdk/... ./services/i18n/sdk/... -v 2>&1 | tail -50
特别确认：
- pub/sub 失效用例
- L1/L2/L3 三层 fallback 用例
- InProcess / Remote 双模式各跑一遍

### 8.4 端到端测试
执行：
  make gateway-e2e
贴出 6001/6003/6005/6007/6009 五条协议的测试结果。

### 8.5 测试用例注册表
贴出 docs/testing/测试用例注册表.md 中本次新增条目（TC-CFG-* / TC-I18N-* / TC-SDK-*）。

================================================================
## 9. 文档验收

### 9.1 CODE-WIKI 更新
列出 docs/wiki/CODE-WIKI.md 中本次新增/修改的章节号与小节号：
- 章节 3 extend map 是否含 client_scope / clientVersion
- 章节 4 协议编号是否含 6009
- 章节 5 routes.yaml 是否含 6009 + Redis pub/sub 失效约定
- 章节 9 是否含 services/config/sdk、services/i18n/sdk 接口说明
- 章节 17 变更日志是否含 2026-05-22 / 2026-05-23 条目

### 9.2 模块文档
- docs/wiki/modules/config-service.md（含 Schema 章节）
- docs/wiki/modules/i18n-service.md（含模板章节）
- docs/wiki/modules/sdk-usage.md（SDK 使用规范）
分别贴出目录结构（一级二级标题）。

### 9.3 ADR
- ADR-008 全局配置与多语言迁移
- ADR-009 Config Schema Registry 与 i18n 模板
- ADR-010 admin 边界与 SDK 引用
分别贴出 Status / Context / Decision / Consequences 四段摘要。

### 9.4 LLM-WIKI
确认 docs/wiki/LLM-WIKI.md 中知识蒸馏条目同步更新。

================================================================
## 10. 禁止事项核查

逐项给出 yes/no + 证据：
- [ ] 是否破坏 6001/6003/6005/6007 wire 兼容
- [ ] 是否服务端做模板渲染（仅推送/短信例外）
- [ ] 是否存在硬编码缓存版本（如 v3）
- [ ] 是否存在单文件 >200 行
- [ ] 是否存在单函数 >50 行
- [ ] 是否业务服务 import services/config 或 services/i18n 内部包
- [ ] 是否业务服务调用 admin-server
- [ ] 是否存在双重语言包读路径
- [ ] 是否存在 git push 操作

================================================================
## 11. 风险与残留项

按 P0/P1/P2 分级列出：
- 已知 bug
- 性能隐患
- 兼容性风险
- 文档缺口
- 后续阶段建议（S2 下线 lang_pack_* 冗余、admin-web 联动等）

================================================================
## 12. 给主控的下一步建议
- 是否可进入 S2 阶段
- 是否需要灰度发布策略
- 是否需要补 ADR

================================================================

请严格按上述结构汇报。
任何项目无证据 = 视为未完成。
任何不匹配此前三轮裁决的实现 = 视为偏差，必须列入风险并给出回滚或修正方案。
不允许 git push，不允许跳过任何一项。
```

---

### **使用说明**

把上面 ```text 包裹的内容**整段复制**发给 Trae，它会按 12 个一级模块、每个模块下若干子项逐条回填。回收 Trae 的报告后，你按下面的判别口径快速过验收：

```text
快速判别口径
─────────────────────────────────────────────
模块 1（目录）        → 任一文件 >200 行 = 阻断
模块 2（协议）        → 6009 未登记 / wire 不兼容 = 阻断
模块 3（数据）        → 业务读路径仍读 lang_pack_* = 阻断
模块 4（services）    → grep 到硬编码 module_key = 阻断
模块 5（SDK）         → 业务服务直接 import 内部包 = 阻断
模块 6（admin 边界）  → 业务服务调用 admin-server = 阻断（最高优先级）
模块 7（闭环）        → 配置/多语言任一闭环未演示 = 阻断
模块 8（测试）        → 覆盖率 <80% 或 e2e 失败 = 阻断
模块 9（文档）        → ADR 缺任一 = 警告，可放 S1 收尾
模块 10（禁止事项）   → 任一 yes = 阻断
─────────────────────────────────────────────
```

如果 Trae 报告中 **模块 6（admin 边界）任一项不通过**，直接打回返工，不接受任何"等下次再改"的妥协 —— 这一条是整套架构的命门，旧项目所有事故几乎都源自这里破防。

收到 Trae 报告后，如果你需要，我可以帮你做 **逐项交叉核对 + 偏差识别 + 修正裁决** 的下一轮。

*内容由 AI 生成仅供参考*