### **Trae 验收结论：5 项阻断 + 5 项 P1 警告，禁止进入 S2。下面给出完整返工指令，按 R0→R5 五批次串行执行，每批必须自检通过后再进入下一批，全部完成后再做一次终验。**

下面这套返工指令直接复制发给 Trae 即可，每一批都附 **修复目标 + 实施约束 + 自检命令 + 通过判据**，避免 Trae 二次跑偏。

***

### **一、对 Trae 检测报告的审阅意见**

#### **1.1 真阻断（必须返工）**

```text
B-01  compose.go 硬编码 base_cfg / pay_cfg switch
      → 这是本次架构决策的核心红线（Schema-driven），违反即架构失败
B-02  9 个文件 >200 行
      → 工程规范红线，CODE-WIKI 已写明
B-03  大量单函数 >50 行（含 redis_cache.go::Invalidate=785 行 这种异常值）
      → 必须先确认是行号误报还是真长函数
B-04  Tars IDL 缺失（config.tars / i18n.tars）
      → routes.yaml 已经引用 ConfigObj/I18nObj，IDL 缺失会导致微服务模式无法编译
B-05（隐含）third_party/{mysqlx,redisx,sqlitex} 目录缺失
      → 此前裁决明确要建，且 SDK Redis pub/sub 依赖 redisx
```

#### **1.2 检测口径需要确认的疑点**

```text
疑点 1：B-03 中"redis_cache.go::Invalidate 785 行 / value.go::Float 1292 行"
       这个数字明显异常（通常是把"行号"当成了"函数行数"）
       要求 Trae 重新汇报：函数起止行号 + 实际函数行数
疑点 2：configs/routes.yaml 实际位于 configs/gateway/routes.yaml
       此前裁决与 CODE-WIKI 引用路径不一致
       要求 Trae 给出最终路径并同步更新文档
疑点 3：6002/6004/6006/6008/6010 这些"返回包协议号"是否真的需要登记
       MessagePacket 模型下，请求/响应共用同一 minType + 是否回包标志位即可
       要求 Trae 复核协议设计是否冗余
```

#### **1.3 P1 警告必须在本轮一并修复**

```text
W-01  i18n/repository 覆盖率 8.5% 是危险信号，必须补到 ≥80%
W-02  ADR-008 缺失 → 整个迁移决策无落档，必须补
W-03  sdk-usage.md 缺失 → 业务接入唯一参考，必须补
W-04  third_party/ 缺失 → 同 B-05
W-05  Tars IDL 缺失 → 同 B-04
```

#### **1.4 模块 6（admin 边界铁律）已通过**

这条是整套架构的命门，Trae 实测 grep 输出为空，业务服务 0 调用 admin-server，可以接受。但返工过程中**绝不允许引入任何 admin-server 依赖**。

***

### **二、给 Trae 的整改指令（整段发给它）**

```text
主控返工裁决：全量验收 5 项阻断 + 5 项 P1 整改

总原则：
- 严格按 R0 → R5 串行执行，每批通过自检后再进入下一批
- 每批完成必须给出 git diff --stat 与每项自检命令的实际输出
- 不允许 git push
- 不允许扩大改动范围（只改本指令明确列出的内容）
- 不允许新增 admin-server 依赖（铁律）
- 不允许破坏 6001/6003/6005/6007 wire 兼容
- 任何与本指令冲突的"看起来更优"做法，先停下来汇报，不得擅自决策

================================================================
# 批次 R0：检测口径核对（先做，10 分钟）

目标：消除上轮检测中的数据疑点，确保后续返工有正确基线。

任务：
0.1 重新汇报"超过 50 行的函数"清单：
    - 给出 文件路径 + 函数名 + 起始行 + 结束行 + 实际行数
    - 不要把"行号"当成"函数行数"
    - 列出 Top 20

0.2 确认 routes.yaml 的最终位置：
    - 是 configs/routes.yaml 还是 configs/gateway/routes.yaml
    - 若是后者，检查 CODE-WIKI、Makefile、main.go 的引用路径是否一致
    - 给出最终决策

0.3 复核协议号 6002/6004/6006/6008/6010 是否真的需要：
    - 给出 docs/api/协议编号注册表.md 中关于"请求/响应是否分开编号"的规范摘录
    - 若 MessagePacket 模型下不需要分开，建议整改方案
    - 不在本轮返工，仅给出建议

通过判据：
- 0.1 输出 Top 20 函数清单
- 0.2 给出最终路径与冲突点列表
- 0.3 给出建议（不实施）

================================================================
# 批次 R1：第三方基础设施补齐（前置）

目标：补齐 third_party/{mysqlx,redisx,sqlitex} 三个基础设施包，
      为后续文件拆分和 SDK 完善提供依赖。

任务：
1.1 创建 go/third_party/mysqlx/
    - mysqlx.go：连接池封装（含健康检查、慢查询日志、超时）
    - mysqlx_test.go：单元测试（用 sqlmock 或集成测试 build tag）
    - 单文件 ≤200 行

1.2 创建 go/third_party/redisx/
    - redisx.go：客户端封装（含健康检查、命名空间隔离）
    - pubsub.go：pub/sub 发布订阅封装（供 SDK 与 admin-server 共用）
    - redisx_test.go：用 miniredis 做单元测试
    - 单文件 ≤200 行

1.3 创建 go/third_party/sqlitex/
    - sqlitex.go：modernc.org/sqlite 封装（in-memory + 文件双模式）
    - schema_adapter.go：MySQL schema → SQLite schema 适配
      （BIGINT AUTO_INCREMENT → INTEGER PK AUTOINCREMENT，JSON → TEXT 等）
    - sqlitex_test.go
    - 单文件 ≤200 行

1.4 现有代码迁移：
    - 业务代码中所有 sql.Open / redis.NewClient 直接调用迁移到这三个包
    - 仅做调用方迁移，不改业务逻辑

1.5 go.work 同步更新（如有需要）

通过判据：
- ls go/third_party/{mysqlx,redisx,sqlitex}/ 三个目录齐全
- go test ./third_party/... 全部 PASS
- grep -rn "sql.Open\|redis.NewClient" go/services/ go/tars/ → 输出为空
- find go/third_party -name "*.go" -not -name "*_test.go" -exec wc -l {} \; → 全部 ≤200

================================================================
# 批次 R2：架构红线修复 - compose.go 去硬编码（最高优先级）

目标：消除 services/config/service/compose.go 中的硬编码 module_key switch，
      改为完全 schema-driven。

任务：
2.1 重新设计 compose.go：
    - 输入：env / client_scope / client_version / 请求的 module_keys
    - 处理：
      a. 从 schema_repo 加载 sys_config_schema（按 module_key 过滤）
      b. 从 value_repo 加载 sys_config_version 当前 published 值
      c. 按 schema.field_type 解析 JSON 值为 TypedValue
      d. 按 schema.client_scope / min_app_ver 过滤
      e. 装配 DynamicConfigModule（dynamic_modules 字段）
    - 强类型字段（base_cfg/wap_cfg/...）填充：
      由"反射 + tag 映射"完成，不允许 switch case
      或：强类型字段全部从 dynamic_modules 中按 module_key 提取，
          再用 protobuf reflection 写入对应 message field
    - 选择更稳的方案：用 reflection + tag

2.2 删除所有硬编码 switch：
    grep -rn "case \"base_cfg\"\|case \"member_cfg\"\|case \"pay_cfg\"" go/services/config/
    必须为空

2.3 拆分 compose.go（当前 340 行）：
    - service/compose.go            主流程（≤120 行）
    - service/compose_dynamic.go    DynamicConfigModule 装配（≤100 行）
    - service/compose_typed.go      强类型字段反射填充（≤100 行）
    - service/compose_filter.go     scope / version 过滤（≤80 行）

2.4 单元测试覆盖：
    - 测试新增 module_key（DB 中存在但 proto 强类型未定义）→ 必须出现在 dynamic_modules
    - 测试新增 field_key（proto 强类型未定义）→ 必须出现在 dynamic_modules
    - 测试 client_scope=android 时仅返回 android 字段
    - 测试 min_app_ver 过滤
    - 测试一个不在 schema 中的 field 在 DB 里 → 降级到默认值

通过判据：
- grep -rn "case \"base_cfg\"\|case \"member_cfg\"\|case \"pay_cfg\"\|case \"wap_cfg\"\|case \"oss_cfg\"\|case \"regex_cfg\"" go/services/config/service/
  → 输出必须为空
- find go/services/config/service -name "compose*.go" -exec wc -l {} \;
  → 全部 ≤200
- go test ./services/config/service/... -count=1 -v
  → PASS，覆盖率 ≥80%

================================================================
# 批次 R3：单文件 ≤200 行 + 单函数 ≤50 行 全量整改

目标：彻底解决 9 个超限文件 + 大量超限函数。

任务：
3.1 文件拆分清单（按 R0 实测列表执行；以下为预期）：
    
    services/config/service/compose.go (340)
      → R2 已处理
    
    services/i18n/repository/sqlite_repo.go (339)
      → 拆为：
        sqlite_repo.go              主入口（≤80 行）
        sqlite_pack_repo.go         lang_pack 操作（≤100 行）
        sqlite_string_repo.go       lang_string 操作（≤100 行）
        sqlite_release_repo.go      lang_pack_release 操作（≤80 行）
    
    services/config/sdk/remote.go (266)
      → 拆为：
        remote.go                   Tars client 封装（≤120 行）
        remote_invoker.go           bytes 接口调用（≤80 行）
        remote_decoder.go           响应解码（≤80 行）
    
    services/config/repository/mem_repo.go (209)
      → 拆为：
        mem_repo.go                 主结构（≤120 行）
        mem_repo_schema.go          schema 操作（≤80 行）
        mem_repo_value.go           value 操作（≤80 行）
    
    其余 5 个超限文件按相同原则拆分。
    每个文件必须按"职责单一 + 命名清晰"拆分，不允许只是机械搬代码。

3.2 长函数拆分（按 R0 Top 20 列表）：
    - 每个函数 ≤50 行
    - 拆分原则：
      a. 提取 helper 函数（命名以小写开头）
      b. 提取 stage（如 validate / normalize / persist / publish）
      c. 不允许通过"加注释折叠"绕过

3.3 拆分前后对照：
    - 给出每个文件拆分前/后的行数表
    - 给出每个函数拆分前/后的行数表

3.4 单元测试不允许失败：
    - 拆分过程不修改对外接口
    - 拆分后所有现有测试必须仍 PASS

通过判据：
- find go/services -name "*.go" -not -name "*_test.go" -exec wc -l {} \; | awk '$1>200'
  → 输出必须为空
- 给出脚本检测所有 ≥50 行函数（可用 gofmt + awk 或 staticcheck 自定义规则）
  → 输出必须为空
- go test ./services/... -count=1
  → 全部 PASS

================================================================
# 批次 R4：Tars IDL 补齐 + 路径与协议号纠偏

目标：补齐 config.tars / i18n.tars，确保微服务模式可编译。

任务：
4.1 创建 tars/protocol/tars/config.tars：
    module CaiRobotConfigApp {
        interface ConfigObj {
            int doRequest(vector<byte> req, out vector<byte> rsp);
        };
    };
    严格遵循 CODE-WIKI 第 9 章 Tars 命名规范。

4.2 创建 tars/protocol/tars/i18n.tars：
    module CaiRobotI18nApp {
        interface I18nObj {
            int doRequest(vector<byte> req, out vector<byte> rsp);
        };
    };

4.3 routes.yaml 路径纠偏（按 R0 0.2 决策）：
    - 选定最终路径
    - 同步更新 CODE-WIKI、Makefile、main.go 引用
    - grep -rn "configs/routes.yaml\|configs/gateway/routes.yaml" 给出输出
    - 必须只剩一种引用

4.4 协议号 6002/6004/6006/6008/6010 处理（按 R0 0.3 决策）：
    - 若决议保留：补充注册表说明
    - 若决议下线：从注册表与 proto 中删除，并在 ADR-008 中记录决策
    - 不允许有"未决定"状态

4.5 TarsGo 代码生成验证：
    - 执行 make proto 或对应 target
    - 给出生成的 .go 文件路径

通过判据：
- ls tars/protocol/tars/{config,i18n}.tars 存在
- TarsGo 代码生成成功
- grep -rn 路径与协议号引用一致
- ADR-008 中有协议号决策记录

================================================================
# 批次 R5：测试覆盖率 + 文档补齐

目标：把 5 个低覆盖率包补到 ≥80%，补齐 2 份缺失文档。

任务：
5.1 测试覆盖率提升（重点）：
    
    i18n/repository (8.5% → ≥80%)
      重点用例：
      - 各仓储接口的正常路径
      - 边界（空结果、超大字符串、并发写）
      - 错误路径（DB 异常、JSON 解析失败）
      - SQLite 与 MySQL 实现的契约一致性测试
    
    config/cache (37.8% → ≥80%)
      重点：Redis 命中、未命中、TTL、pub/sub 失效、L1 LRU 容量边界
    
    i18n/cache (39.7% → ≥80%)
      重点同上
    
    config/repository (38.3% → ≥80%)
      重点：schema CRUD、value CRUD、版本号自增、并发安全
    
    config/service (53.1% → ≥80%)
      重点：compose 各分支、schema 校验失败路径、过滤逻辑

5.2 补齐文档：

    docs/wiki/adr/ADR-008-config-i18n-migration.md
      内容：
      - Status: Accepted
      - Context: 旧项目双重存储、单文件超限、缓存硬编码三大问题
      - Decision: 单一数据源 + Schema Registry + SDK + admin 边界
      - Consequences: 破坏点、迁移期镜像、客户端兼容策略
      - 记录协议号 6002/6004/6006/6008/6010 的最终决策（来自 R4.4）

    docs/wiki/modules/sdk-usage.md
      内容：
      - configsdk / i18nsdk 引用方式
      - InProcess / Remote 双模式如何选择
      - Watch / 失效语义
      - 三层缓存模型
      - 业务服务接入示例（OpenAPI / 设备网关 / 用户中台）
      - 禁止事项（不得 import 内部包、不得调用 admin-server）

5.3 LLM-WIKI 同步：
    docs/wiki/LLM-WIKI.md 增加 SDK 与 admin 边界条目。

5.4 CODE-WIKI 同步：
    第 5、9、17 章按之前裁决更新。

通过判据：
- go test ./services/... -coverprofile=cover.out
  go tool cover -func=cover.out | awk '{if($3+0<80 && $1!="total:") print}'
  → 输出必须为空
- ls docs/wiki/adr/ADR-008*.md docs/wiki/modules/sdk-usage.md → 存在
- CODE-WIKI、LLM-WIKI 章节同步

================================================================
# 终验：再跑一次完整验收清单

完成 R0~R5 后，重新执行此前的"全量验收报告"模板，
逐项给出新证据。重点确认：

1. 模块 1：所有文件 ≤200 行、所有函数 ≤50 行
2. 模块 4：grep 不到任何 case "xxx_cfg" 硬编码
3. 模块 5：SDK 接口与三层缓存完整
4. 模块 6：admin 边界铁律仍然成立
5. 模块 8：所有包覆盖率 ≥80%
6. 模块 9：ADR-008/009/010 + sdk-usage.md 全部齐备
7. 模块 10：禁止事项全部 NO

终验通过后给出：
- git diff --stat（所有改动汇总）
- go test ./... -count=1 全量结果
- go test ./... -cover 覆盖率快照
- 风险与残留项（应为空或仅剩 P2 建议）

================================================================
# 全程禁止事项

1. 不允许 git push
2. 不允许新增 admin-server 依赖
3. 不允许破坏 6001/6003/6005/6007 wire 兼容
4. 不允许跳过任何一批，必须 R0→R5 串行
5. 不允许擅自决策 R0 中的 3 个疑点，必须先汇报
6. 不允许通过"屏蔽 lint 规则"或"加注释"绕过行数限制
7. 不允许在 compose.go 用任何 module_key 字符串字面量做分支判断
8. 不允许扩大改动范围到本指令未列项

================================================================
# 汇报格式

每完成一批，请按以下结构汇报：

## 批次 Rx 完成报告

### 1. 改动文件清单（git diff --stat）
### 2. 关键代码摘录（每个新增/修改文件 ≤30 行核心片段）
### 3. 自检命令与实际输出
### 4. 通过判据逐项打勾
### 5. 遗留问题（应为空）
### 6. 是否可进入下一批

主控收到后会做交叉核对，通过后再发下一批指令。
请从 R0 开始执行。
```

***

### **三、给你的并行决策事项**

在 Trae 跑 R0 的同时，你需要确认两件事，否则 R4 会卡住：

```text
决策 1：routes.yaml 最终位置
  推荐：configs/gateway/routes.yaml
  理由：与 go/gateway/proto-gateway 物理位置呼应，
        未来如果有 configs/admin/、configs/devicegw/ 等扩展更整齐
  动作：在 R0 0.2 中告诉 Trae 选这个，并同步更新 CODE-WIKI

决策 2：6002/6004/6006/6008/6010 是否保留
  推荐：下线
  理由：MessagePacket 模型下，请求与响应共用同一 minType，
        通过 MessagePacket.is_response 或 result.code 区分即可，
        多登记一倍编号反而污染注册表，与"快速增加协议"的目标背道而驰
  动作：在 R0 0.3 中告诉 Trae 选下线方案，
        在 R4.4 中执行删除并写入 ADR-008
```

如果你认可这两条决策，把这两段直接附在我给 Trae 的指令后面发过去；如果你有其他偏好，告诉我，我来调整 R4。

***

### **四、节奏控制建议**

```text
R0  10 分钟 → 你审核疑点回复（最关键，决定 R4 走向）
R1  半天    → third_party 三件套 + 现有调用迁移
R2  1 天    → compose.go 去硬编码（架构核心）
R3  1 天    → 文件 + 函数全量拆分
R4  半天    → Tars IDL + 路径 + 协议号纠偏
R5  1 天    → 测试覆盖率 + 文档
终验 半天   → 全量验收 + 风险归档

总周期：约 4 个工作日
```

每批结束后让 Trae 停下来汇报，**绝不允许它把 R0\~R5 一口气跑完再来汇报**——这种长任务一旦中间跑偏，回滚成本极高。R2 是最容易跑偏的一批（去硬编码涉及 reflection + protobuf descriptor，Trae 可能会偷懒退化为某种 map switch），如果 R2 汇报里看到任何 `switch moduleKey` 字样，**立刻打回**。

收到 Trae 的 R0 汇报后，把它发给我，我会做交叉核对并决定是否放行 R1。

*内容由 AI 生成仅供参考*
