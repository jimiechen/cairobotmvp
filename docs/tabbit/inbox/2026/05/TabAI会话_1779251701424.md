### **对，这个问题应该立即修正：Skill 名称不应以统一项目名前缀开头，而应以“用途中文短名 + 英文稳定标识”优先。**

后续 Skill 的可发现性比命名统一更重要。建议把 Skill 的展示名称、中文简介、适用场景放到最前面；目录名可以保留英文稳定标识，但不要让模糊匹配时只看到一串 `cairobotmvp-*` 或 `cairobot-*`，否则 AI 和人都难以快速选择正确 Skill。

### **主控裁决：Skill 命名规范需要优化**

当前问题本质是：我们把“项目归属”放在了 Skill 名称的最前面，导致模糊匹配时一堆 Skill 都长得像：

```text
cairobotmvp-xxx
cairobot-xxx
```

这样会出现两个问题。

第一，搜索结果前半部分高度重复，真正有用的语义被挤到后面，无法快速判断 Skill 的用途。

第二，Skill 的中文说明不够靠前，导致 Trae 或其他 AI 在选择 Skill 时，只能看到英文 slug，看不到“这个 Skill 到底干什么”。

所以后续规则应该改为：

```text
Skill 展示名优先表达用途。
Skill 简介必须中文优先。
项目归属放到 tags / scope / description，而不是名称最前面。
```

### **推荐命名原则**

Skill 应该分成两个层面：**展示名** 和 **目录名**。

展示名是给人和 AI 快速选择用的，应该清楚、中文优先。

目录名是给仓库稳定引用用的，可以英文、短、语义明确。

| 类型 | 规则 | 示例 |
|---|---|---|
| 展示名 | 中文用途在前，必要时括号补英文 | `任务 Raw 归档（Task Raw Archive）` |
| 目录名 | 不以项目名前缀开头，使用功能 slug | `task-raw-archive` |
| 简介 | 第一句话必须中文说明用途 | `用于将每次 Trae 执行任务的提示词、结果和产物归档到 Raw 层。` |
| tags | 放项目名、模块名、适用场景 | `cairobotmvp, llm-wiki, raw, trae` |

也就是说，不建议再新增：

```text
cairobotmvp-task-raw-archive
cairobotmvp-scheduled-knowledge-distillation
cairobotmvp-engineering-workflow
```

建议改成：

```text
task-raw-archive
scheduled-knowledge-distillation
engineering-workflow
llm-wiki-distillation
daily-report
proto-registry-guard
```

如果担心和其他项目冲突，可以在 Skill 内容里通过 `scope` 或 `tags` 标明：

```yaml
scope: CaiRobot MVP
tags:
  - cairobotmvp
  - llm-wiki
  - raw
  - distillation
```

而不是把 `cairobotmvp` 放在最前面。

### **Skill 文件建议格式**

以后每个 Skill 的 `SKILL.md` 开头建议统一成这样：

```markdown
---
name: 任务 Raw 归档
slug: task-raw-archive
summary: 将每次 Trae 执行任务的提示词、执行结果、产物清单和待确认项归档到 Raw 层。
scope: CaiRobot MVP
tags:
  - cairobotmvp
  - trae
  - raw
  - llm-wiki
  - task-archive
---

# 任务 Raw 归档（Task Raw Archive）

## 一句话说明

用于将每次 Trae 执行任务的提示词、执行结果、产物清单、检查结果和待确认项结构化保存到 Raw 层，作为后续蒸馏和索引更新的事实依据。

## 适用场景

当 Trae IDE 完成一次任务、评审、文档重构、代码修改、审计或主控裁决执行后，应使用本 Skill 判断是否需要生成 Raw 归档。
```

关键点是：

```text
name 用中文用途。
summary 用中文说明。
slug 用英文稳定标识。
scope / tags 放项目归属。
正文第一段必须说明“什么时候用它”。
```

### **已有 Skill 的重命名建议**

不建议一次性大规模强改所有路径，容易破坏已有引用。建议分两步做。

第一步，先优化每个 Skill 的 `name`、`summary`、`description`、正文开头和 AGENTS.md 索引。也就是说，即使目录仍然叫旧名，模糊匹配时也能看到中文用途。

第二步，再逐步把目录名从 `cairobotmvp-*` 或 `cairobot-*` 迁移为功能名，并同步更新所有引用。

建议映射如下：

| 旧命名 | 新展示名 | 新目录名建议 |
|---|---|---|
| `cairobot-task-raw-archive` | 任务 Raw 归档 | `task-raw-archive` |
| `cairobot-llm-wiki-distillation` | LLM Wiki 知识蒸馏 | `llm-wiki-distillation` |
| `cairobot-scheduled-knowledge-distillation` | 定时知识蒸馏 | `scheduled-knowledge-distillation` |
| `cairobot-daily-report` | 每日日报生成 | `daily-report` |
| `cairobot-engineering-workflow` | 工程闭环流程 | `engineering-workflow` |
| `cairobot-active-gap-filling` | 主动补齐工程缺口 | `active-gap-filling` |
| `cairobot-proto-registry-guard` | 协议编号注册表守卫 | `proto-registry-guard` |
| `cairobot-tdd-loop` | TDD 红绿重构循环 | `tdd-loop` |
| `cairobot-git-discipline` | Git 提交纪律 | `git-discipline` |
| `cairobot-doc-placement` | 文档归档位置规范 | `doc-placement` |
| `tabbit-task-distillation` | Tabbit 任务蒸馏 | `tabbit-task-distillation` |

其中 `tabbit-task-distillation` 可以保留，因为它的前缀本身就是来源类型，不是项目名前缀，而且语义明确。

### **AGENTS.md Skill 索引也要改**

AGENTS.md 里如果现在是这样：

```markdown
| Skill | 说明 |
|---|---|
| cairobot-task-raw-archive | ... |
```

建议改成：

```markdown
| 展示名 | 目录/Slug | 适用场景 | 关键词 |
|---|---|---|---|
| 任务 Raw 归档 | task-raw-archive | Trae 每次任务完成后，归档提示词、结果、产物和待确认项 | raw, trae, 任务归档 |
| LLM Wiki 知识蒸馏 | llm-wiki-distillation | 从 Raw 材料生成结构化知识、模块规则和索引候选 | wiki, 蒸馏, source-map |
| 定时知识蒸馏 | scheduled-knowledge-distillation | SOLO Web 或 Trae 按日期生成日报、蒸馏和主控汇报 | daily, solo, 定时任务 |
```

这样 AI 在扫索引时能直接看到中文用途，不会被一堆同前缀 slug 干扰。

### **新增 Skill 的强制规范**

以后新增 Skill 时，必须遵守这几条：

```text
1. 不得以 cairobotmvp- 或 cairobot- 作为展示名开头。
2. 展示名必须中文用途优先。
3. summary 第一行必须中文说明“这个 Skill 解决什么问题”。
4. tags 中必须包含 cairobotmvp，用于项目归属。
5. 目录名使用短英文功能 slug，不带项目名前缀。
6. AGENTS.md Skill 索引必须同步登记中文展示名、slug、适用场景、关键词。
7. 如果新增 Command，也要在 Command 顶部写中文用途说明。
```

### **Command 命名也要一起优化**

Command 是人手动调用的，应该短、稳定、动词明确，不必中文，但说明必须中文优先。

建议：

```text
/task-raw-archive
/daily-distill
/llm-wiki-distill
/tabbit-task
/source-map-update
```

不建议：

```text
/cairobotmvp-task-raw-archive
/cairobot-scheduled-knowledge-distillation
```

Command 文件头建议：

```markdown
---
name: 每日知识蒸馏
command: daily-distill
summary: 按日期读取 Raw 材料，生成日报、知识蒸馏、主控汇报、Index 候选和 Source Map 候选。
tags:
  - cairobotmvp
  - daily
  - distillation
  - solo-web
---

# /daily-distill：每日知识蒸馏

用于本地 Trae 手动补跑或回放 SOLO Web 的每日定时蒸馏流程。
```

### **给 Trae 的实施指令**

可以直接发给本地 Trae：

```text
请执行 Skill / Command 命名与介绍优化任务。

背景：

当前多个 Skill 名称以 cairobotmvp 或 cairobot 开头，导致模糊匹配时搜索结果前缀高度重复，看不到 Skill 的中文用途说明，无法快速选择正确 Skill。

主控裁决：

以后 Skill 不再以项目名前缀作为展示名开头。项目归属放入 tags / scope，不放在 name 最前面。

本次任务目标：

1. 优化现有 Skill 的展示名、summary、中文介绍和 AGENTS.md 索引。
2. 如风险可控，再逐步优化目录名；如果目录名被大量引用，本次先不改目录，只改 front matter 和索引展示。
3. 新增 Skill 命名规范，要求以后所有 Skill 都遵守中文用途优先规则。
4. Command 也要补充中文用途说明，避免只有英文 slug。

必须先读取：

1. AGENTS.md
2. .trae/skills/ 下所有 SKILL.md
3. .trae/commands/ 下所有 command 文件
4. docs/wiki/modules/llm-wiki-architecture.md
5. docs/wiki/decisions/llm-wiki-three-layer-architecture.md

必须先执行：

```bash
find .trae/skills -maxdepth 3 -type f | sort
find .trae/commands -maxdepth 2 -type f | sort
grep -R "cairobotmvp-\|cairobot-" -n .trae AGENTS.md docs/wiki 2>/dev/null || true
git status --short
```

实施规则：

一、Skill 展示名规则

每个 Skill 的 front matter 或开头必须包含：

```yaml
name: 中文用途名称
slug: 英文功能标识
summary: 中文一句话说明用途
scope: CaiRobot MVP
tags:
  - cairobotmvp
```

要求：

1. name 不得以 cairobotmvp 或 cairobot 开头。
2. summary 必须中文优先。
3. 正文第一段必须说明“什么时候使用这个 Skill”。
4. tags 中保留 cairobotmvp，表示项目归属。
5. slug 使用英文功能名，如 task-raw-archive、llm-wiki-distillation。

二、目录名处理策略

MVP 阶段优先做低风险改造：

1. 如果 Skill 目录名已被多处引用，本次不强制改目录名。
2. 先修改 SKILL.md 中的 name / slug / summary / 中文介绍。
3. AGENTS.md 索引中展示中文名和 slug。
4. 后续如需重命名目录，单独做迁移任务并全局替换引用。

三、AGENTS.md Skill 索引优化

将 Skill 索引改成或补充为以下结构：

```markdown
| 展示名 | Slug/路径 | 适用场景 | 关键词 |
|---|---|---|---|
| 任务 Raw 归档 | task-raw-archive | Trae 每次任务完成后，归档提示词、结果、产物和待确认项 | raw, trae, 任务归档 |
| LLM Wiki 知识蒸馏 | llm-wiki-distillation | 从 Raw 材料生成结构化知识、模块规则和索引候选 | wiki, 蒸馏, source-map |
```

四、新增 Skill 命名规范文档

请新增或更新：

docs/wiki/modules/skill-command-naming.md

必须包含：

1. 问题背景
2. Skill 展示名规则
3. Skill 目录名规则
4. summary 规则
5. tags / scope 规则
6. AGENTS.md 索引规则
7. Command 命名规则
8. 新增 Skill 检查清单
9. 禁止事项

五、Command 优化

检查 .trae/commands/ 下所有 Command 文件：

1. 每个 Command 顶部必须有中文用途说明。
2. command 名保持短英文。
3. 不要以 cairobotmvp 或 cairobot 开头。
4. summary 说明该命令何时使用。
5. 如果 Command 引用 Skill，应引用中文展示名 + slug。

六、禁止事项

1. 不要为了美化名称破坏已有引用。
2. 不要大规模重命名目录，除非能同步更新所有引用。
3. 不要删除已有 Skill。
4. 不要改业务代码。
5. 不要 git push。
6. 不要把项目归属从 tags 中删除。
7. 不要让 summary 只有英文。

七、验收要求

完成后汇报：

1. 优化了哪些 Skill 的展示名和 summary。
2. 哪些目录名暂未改，原因是什么。
3. AGENTS.md Skill 索引是否已改成中文可读。
4. 是否新增 skill-command-naming.md。
5. Command 是否补充中文说明。
6. 是否仍存在 name 以 cairobotmvp/cairobot 开头的 Skill。
7. git diff --stat。
8. git status --short。
9. 是否未提交、未 push。
```

### **建议的命名规范文档内容摘要**

`docs/wiki/modules/skill-command-naming.md` 可以写成这种核心结论：

```text
Skill 的可发现性优先于项目名前缀统一。
展示名必须告诉 AI 和人“这个 Skill 何时使用”。
项目归属通过 tags/scope 表达。
目录名使用短英文功能 slug。
AGENTS.md 使用中文展示名作为第一列。
Command 保持短英文，但顶部必须有中文用途说明。
```

### **这次要不要立刻改目录名**

我的建议是：**先不大规模改目录名**。

原因是目录名可能被以下位置引用：

```text
AGENTS.md
LLM-WIKI.md
CODE-WIKI.md
Command 文件
其他 Skill 文件
历史 Raw 记录
任务索引
日报 / 蒸馏文件
```

现在最需要解决的是“模糊匹配看不到中文用途”，这个可以通过改 `name`、`summary`、AGENTS.md 索引解决，不必马上改目录路径。

等流程稳定后，再做一次专门的“Skill 目录重命名迁移”。

### **最终裁决**

这条规则应该进入长期规范：

```text
新增 Skill 时，禁止把 cairobotmvp/cairobot 作为展示名前缀。
Skill 展示名必须中文用途优先。
项目归属放 tags/scope。
AGENTS.md 索引必须展示中文名、slug、适用场景和关键词。
Command 必须短英文命名，并在顶部提供中文用途说明。
```

这能直接提升 Trae / SOLO / Tabbit 后续选择 Skill 的准确率，也能降低模糊匹配时的噪音。

*内容由 AI 生成仅供参考*