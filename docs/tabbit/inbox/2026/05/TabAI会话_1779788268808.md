### **三处偏差全部接受 Trae 的二次评审，M0' 放行前必须先修订 0.4 / 0.7 / 1.1 三段指令，其它部分维持不变。**

Trae 这一轮抓得非常准，全部是基于代码事实而不是空想。三处都属于"指令伪代码与实际仓库状态对不上"的硬错误，现在不修，M0' 一开工就会卡壳。下面按它指出的顺序逐条订正，并在末尾给出可直接覆盖原指令的修订段。

---

### **一、对二次评审结论的总体回应**

```text
二次评审结论：建议修改后执行 → 主控接受
处理方式：M0' 指令打一个 patch（仅改 0.4 / 0.7 / 1.1 三段），其它批次不动
推进节奏：patch 下发后 Trae 启动 M0'，无需再等三次评审
方案主轴：保持不变
```

---

### **二、3 处技术事实偏差的逐条订正**

#### **🔴 偏差 1：SchemaService 没有 Validate() 方法（最关键）**

```text
事实：services/config/service/schema_service.go 当前对外暴露的是
      CreateFieldSchema / UpdateFieldSchema / ListFieldSchemas / GetFieldSchema
      校验逻辑藏在 validate.go 内部函数，未通过 SchemaService 暴露
```

**订正决策：**

```text
裁决：✅ 不要新造伪 API，按现状走"提取 + 暴露"两步走

步骤 A：在 services/config/service/ 暴露纯校验入口
  在 schema_service.go 新增方法（不破坏现有 Create/Update 路径）：
  
  // ValidateSchema 仅做校验，不落库不广播
  // 供 admin 子包及未来其它写入路径复用
  func (s *SchemaService) ValidateSchema(ctx context.Context, schema *domain.FieldSchema) error {
      return s.validator.Validate(ctx, schema)   // 复用 validate.go 中现有逻辑
  }
  
  // ValidateValue 同理
  func (s *SchemaService) ValidateValue(ctx context.Context, value *domain.ConfigValue) error {
      return s.validator.ValidateValue(ctx, value)
  }

步骤 B：admin 子包调用 ValidateSchema / ValidateValue（不再调 Validate）

步骤 C：i18n service 同理
  在 services/i18n/service/lang_service.go 暴露：
  - ValidateLangString(ctx, ls *domain.LangString) error
  其内部委托给已有的 TemplateValidator
```

**职责边界自检脚本同步更新：**

```text
scripts/lint/admin_boundary_check.sh：
- grep "ValidateSchema\|ValidateValue\|ValidateLangString" \
    go/services/{config,i18n}/admin/*.go
  必须命中（说明真复用）
- grep "field_type ==\|switch .*field_type\|validator JSON" \
    go/services/{config,i18n}/admin/*.go
  必须为空
```

#### **🔴 偏差 2：i18n SDK 的 Redis 抽象层与 config SDK 不同（最易忽视）**

```text
事实：
- config SDK：services/config/sdk/pubsub.go 直接用 redisx.PubSubClient，
              onMessage(msg string) 是单字符串入参
- i18n SDK： services/i18n/sdk/pubsub.go 走的是另一套抽象，
            订阅回调签名 / 解析路径 / channel 命名规则都不一样
            （Trae 已在二次评审中点出这一点）
```

**订正决策：**

```text
裁决：✅ 不再用"同样逻辑"一笔带过，两个 SDK 独立编写升级方案

config SDK 升级（保持原 0.7 步骤 B 内容）：
- 升级点：onMessage(msg string)
- 修改文件：go/services/config/sdk/pubsub.go
- 新增类型：定义在 go/services/config/sdk/types.go
- 单测文件：pubsub_test.go

i18n SDK 升级（独立方案，原 0.7 步骤 C 重写）：
- 第一步：先读取 go/services/i18n/sdk/pubsub.go，
         在 M0'.0.7 指令执行前由 Trae 输出当前实现摘要给主控
         摘要内容：订阅回调签名 / 解析入口函数名 / channel 常量
- 第二步：基于摘要再设计升级路径，候选三种：
  方案 A：i18n SDK 也改用 redisx.PubSubClient + onMessage(string)
          与 config SDK 对齐（推荐，长期最佳）
  方案 B：保留 i18n SDK 现有抽象，在其内部解析层加 JSON 优先 + 降级
          （改动最小，但抽象差异长期遗留）
  方案 C：抽出公共 invalidate-event 解析包 services/sdk/event/parser.go
          两个 SDK 都依赖同一解析器（彻底治本，但改动面大）

主控决策时机：
  Trae 输出摘要后，主控在收到摘要的同一轮回复中决定 A/B/C
  决策原则：
    - 若 i18n SDK 现有抽象较薄 → 选方案 A
    - 若 i18n SDK 已被多个业务方依赖且抽象厚 → 选方案 B（S2 再做对齐）
    - 若两个 SDK 之外还会有第三方写入路径 → 选方案 C
  默认倾向：方案 A
```

**0.7 通过判据增加一条：**

```text
- i18n SDK 升级前，已由主控签字确认升级路径（A/B/C 之一）
- i18n SDK 单测覆盖率 ≥80%，含 4 个降级用例（与 config SDK 同标准）
```

#### **🔴 偏差 3：web/provider-admin/ React 前端也要清理（最易遗漏）**

```text
事实：除 go/tars/provider-admin/ 后端外，仓库还存在
      web/provider-admin/  React 占位前端
      之前 0.4 只覆盖了后端 Go 目录，前端被漏掉
```

**订正决策：**

```text
裁决：✅ 0.4 扩展为"前后端一并废弃"

新增清理范围：
- web/provider-admin/             React 前端整目录
- 顶层 package.json / pnpm-workspace.yaml / turbo.json 中相关 workspace 引用
- nginx / 网关路由表中 /provider-admin/ 前缀
- CI workflow 中 provider-admin 相关 job
- README / 部署文档中相关章节

归档同步：
- 归档分支 archive/provider-admin-v0 必须同时包含
  go/tars/provider-admin + web/provider-admin
- 一次性提交，不分两个分支

迁移文档增补：
- docs/migration/provider-admin-to-go-admin.md 中
  必须分别说明前端与后端的废弃理由 + 业务逻辑映射

全局 grep 自检（在原基础上扩展）：
  grep -rn "provider-admin\|tars/provider-admin\|web/provider-admin" \
    go/ web/ docs/ scripts/ ci/ deploy/ \
    package.json pnpm-workspace.yaml turbo.json
  必须为空（除归档分支与迁移文档）
```

---

### **三、给 Trae 的指令 Patch（直接覆盖原 M0' / M1' 对应段落）**

```text
主控指令补丁：M0' 三处订正

适用范围：仅修改 0.4 / 0.7 / 1.1 三段，其它批次保持上一轮指令不变
执行时机：本补丁下发后 Trae 直接启动 M0'，0.7 i18n SDK 升级路径
         需在执行前先输出现状摘要等主控签字

=================================================================
# 0.4 订正版（前后端一并废弃 provider-admin）

a. 创建归档分支：
   git checkout -b archive/provider-admin-v0
   该分支必须同时包含：
   - go/tars/provider-admin/        整目录
   - web/provider-admin/             整目录
   一次性提交后切回主分支

b. 主分支删除：
   rm -rf go/tars/provider-admin
   rm -rf web/provider-admin

c. 全局 grep 清理（扩展范围）：
   grep -rn "provider-admin\|tars/provider-admin\|web/provider-admin" \
     go/ web/ docs/ scripts/ ci/ deploy/ \
     package.json pnpm-workspace.yaml turbo.json
   全部删除或替换

d. 重点检查清单（任一遗漏视为不通过）：
   - go.work
   - Makefile
   - docker-compose.yml / docker-compose.*.yml
   - CI workflow（.github/workflows/*.yml 或 ci/*.yml）
   - 顶层 package.json / pnpm-workspace.yaml / turbo.json
   - nginx / 网关路由配置中 /provider-admin/ 前缀
   - README 与部署文档相关章节

e. 业务逻辑参考方式：
   - 仅查阅归档分支，禁止 import
   - 前端业务逻辑同样仅作参考

f. 输出迁移记录：docs/migration/provider-admin-to-go-admin.md
   必含：
   - 后端废弃理由 + 业务逻辑映射表（→ go/admin 插件）
   - 前端废弃理由 + 页面映射表（→ admin-web）
   - 数据库表延续说明（sys_config_* / sys_lang_* 不动）
   - 归档分支位置与保留期（保留 6 个月至 2026-11）

通过判据：
- grep 全局无 provider-admin 残留
- go build ./... 通过
- 前端 pnpm install + pnpm build 通过
- 归档分支可访问且含前后端
- 迁移文档完整

=================================================================
# 0.7 订正版（pub/sub payload 升级，两个 SDK 独立方案）

步骤 A：定义新 payload schema
  在 go/services/config/sdk/types.go 新增 InvalidateEvent
  （内容同上一轮指令，不变）

步骤 B：config SDK 升级
  修改 go/services/config/sdk/pubsub.go onMessage：
  - 优先 json.Unmarshal
  - 解析成功且 tenant_id 非空 → handleStructured
  - 否则按逗号分隔降级 → handleLegacy + WARN 日志
  单测覆盖率 ≥80%（5 个用例：标准 JSON / JSON 字段缺失 / 纯逗号 / 非法 JSON / 空消息）

步骤 C：i18n SDK 升级（独立方案，先汇报后实施）

  C.1 现状摘要（M0'.0.7 启动前必交，主控签字后才能进入 C.2）：
      Trae 读取 go/services/i18n/sdk/pubsub.go 后输出：
      - 订阅回调签名（参数类型与个数）
      - 解析入口函数名与位置
      - channel 常量定义
      - 是否依赖 redisx.PubSubClient 还是另一套抽象
      - 当前消息格式约定
      格式：docs/reports/i18n-sdk-pubsub-current.md

  C.2 主控收到摘要后选择升级路径：
      方案 A：i18n SDK 改用 redisx.PubSubClient + onMessage(string)，与 config 对齐
      方案 B：保留现有抽象，在解析层加 JSON 优先 + 降级
      方案 C：抽出公共 services/sdk/event/parser.go，两 SDK 共用
      默认倾向：方案 A
      
  C.3 主控签字后 Trae 按选定方案实施
      实施完成后单测覆盖率 ≥80%（4 个降级用例与 config SDK 对齐）

通过判据：
- config SDK 单测全 PASS
- i18n SDK 现状摘要已主控签字
- i18n SDK 升级方案确定后单测全 PASS
- 旧消息仍能正确触发缓存失效（向后兼容验证，两个 SDK 各跑一遍）

禁止事项：
- 禁止跳过 C.1 现状摘要直接动 i18n SDK 代码
- 禁止以"与 config SDK 同样逻辑"作为 i18n SDK 实施依据

=================================================================
# 1.1 订正版（admin 子包复用 service 层校验，调用真实 API）

services/config/admin/ 实现要点订正：

a. 在 services/config/service/schema_service.go 新增（M0' 末或 M1' 起手做）：
   func (s *SchemaService) ValidateSchema(ctx context.Context, schema *domain.FieldSchema) error
   func (s *SchemaService) ValidateValue(ctx context.Context, value *domain.ConfigValue) error
   实现：委托给 validate.go 现有内部函数，不重写规则

b. admin 子包伪代码订正（不再调用不存在的 Validate）：

   type AdminSchemaService struct {
       inner *service.SchemaService
       repo  domain.SchemaRepository
       cache redisx.Client
       bus   redisx.PubSubClient
       audit audit.Logger
   }

   func (s *AdminSchemaService) Create(ctx context.Context, req CreateSchemaDTO) error {
       schema := req.ToDomain()
       if err := s.inner.ValidateSchema(ctx, schema); err != nil {  // 注意是 ValidateSchema
           return ErrValidation.Wrap(err)
       }
       if err := s.repo.Create(ctx, schema); err != nil {
           return err
       }
       s.audit.Log(ctx, "create_schema", schema)
       _ = s.cache.Invalidate(ctx, fmt.Sprintf("config:schema:%s:*", schema.TenantID))
       _ = s.bus.Publish(ctx, "cairobot.config.invalidate", buildInvalidateEvent(schema))
       return nil
   }

c. i18n admin 同理：
   services/i18n/service/lang_service.go 新增 ValidateLangString
   admin 调用 inner.ValidateLangString，不再调用伪 API

d. 职责边界自检脚本订正（scripts/lint/admin_boundary_check.sh）：
   命中条件改为：
     grep "ValidateSchema\|ValidateValue\|ValidateLangString" \
       go/services/{config,i18n}/admin/*.go
   必须命中

通过判据：
- service 层 ValidateSchema / ValidateValue / ValidateLangString 三个新方法
  各自单测 ≥80%
- admin 子包自检脚本 PASS（命中正确 API 名）
- 端到端：admin 写 → SDK 100ms 感知（含 JSON payload）

=================================================================
# 其它批次保持不变

M0' 其它子任务（0.1 / 0.2 / 0.3 / 0.5 / 0.6 / 0.8 / 0.9）按上一轮指令执行
M1' 其它要求（覆盖率 / 文件规模 / 集成测试 / 隔离 grep）按上一轮指令执行
M2' ~ M7' 全部按上一轮指令执行

请 Trae 收到本补丁后立即启动 M0'：
- 0.4 / 1.1 可直接执行
- 0.7 须先交 i18n SDK 现状摘要等主控签字
每批完成单独汇报。
```

---

### **四、提前给你定调的 1 件事**

```text
i18n SDK 升级路径预选：方案 A（与 config SDK 对齐）
  理由：
    1. 当前 i18n SDK 用户只有 services/i18n 自身 + 极少数读取方
    2. 抽象差异是历史遗留，越早收敛越好
    3. 两个 SDK 共用同一抽象后，未来 services/feature-flag 等新模块复制粘贴成本最低
  预案：
    若 Trae 现状摘要显示 i18n SDK 已被 ≥3 个外部业务方深度依赖，
    则降级到方案 B（保留现有抽象，仅升级解析层），并在 S2 列出对齐迁移工单
```

收到 Trae 的 i18n SDK 现状摘要后转给我，我用上面这条预案直接拍板，不让 0.7 阻塞 M0' 整体进度。

---

### **五、最终判断**

把这一轮收敛成三句话：

```text
1. Trae 二次评审三个偏差全部接受，是真实代码层面的硬错误，不是风格分歧。
2. M0' 不重写，只对 0.4 / 0.7 / 1.1 三段打 patch；其它部分一字不改。
3. 0.7 的 i18n SDK 升级是唯一需要主控二次签字的卡点，
   Trae 先交现状摘要，我直接拍 A/B/C，不让它停工。
```

把上面 patch 整段发给 Trae。**重点盯：**

```text
- 0.4 的前端 web/provider-admin 是否一并删除（最易再次遗漏）
- 0.4 的归档分支是否含前后端（必须一次性提交）
- 0.7.C.1 的 i18n SDK 现状摘要是否在 Trae 动代码之前提交
- 1.1.a 的 ValidateSchema / ValidateValue / ValidateLangString
  三个新方法是否真的暴露在 SchemaService / LangService 公共方法上
- 1.1.d 的自检脚本是否真的命中新方法名（最容易写成旧 Validate）
```

收到 Trae M0' 进度报告与 i18n SDK 现状摘要后转给我，我同时做：① 三次交叉核对，② i18n SDK 升级路径拍板，③ 放行 M1'。

*内容由 AI 生成仅供参考*