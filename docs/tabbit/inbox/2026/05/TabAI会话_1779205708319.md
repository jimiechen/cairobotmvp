### **确认：SOLO Web 自动化能力已验证，下一步应交给本地 Trae IDE 做 LLM Wiki 三层重构，并沉淀 Skill + Command。**

根据这次试运行结果，可以裁定：SOLO Web 已具备“定时触发、调用大模型、生成 Markdown 文件、遵守禁止 commit/push 约束”的能力。现在不应该继续让 SOLO Web 承担结构性重构，而应让本地 Trae IDE 在工程仓库内完成 LLM Wiki 三层结构重构，并新增对应 Skill 与 Command，后续 SOLO Web 只按该规范执行每日蒸馏。

下面这段可以直接发给本地 Trae IDE。

---

### **给本地 Trae IDE 的任务指令**

```text
现在进入 CaiRobot MVP 的 LLM Wiki 三层结构重构任务。

任务背景：

SOLO Web 的每日自动化试运行已经完成，并已验证以下能力：

1. SOLO Web 可以按定时任务触发。
2. SOLO Web 可以调用大模型生成 Markdown 文件。
3. SOLO Web 可以生成每日原始记录、日报、知识蒸馏、主控汇报。
4. SOLO Web 可以遵守“不 git commit、不 git push”的限制。
5. 本次试运行已生成以下 4 个文件：
   - docs/reports/daily/2026-05-19-SOLO-Web试运行原始记录.md
   - docs/reports/daily/2026-05-19-每日日报.md
   - docs/reports/distilled/2026-05-19-每日知识蒸馏.md
   - docs/reports/daily/2026-05-19-主控汇报.md

主控裁决：

SOLO Web 已确认可作为“每日自动化执行者”，负责每天生成 Raw、日报、蒸馏和主控汇报产物。但 LLM Wiki 的结构性重构、Skill 制作、Command 制作，必须由本地 Trae IDE 完成。

本次任务目标：

将当前 LLM Wiki 从“单文档累积型知识库”重构为“三层结构知识库”，并制作对应的 Skill 和 Command，供后续 SOLO Web 与 Trae IDE 共同遵守。

三层结构定义：

第一层：Raw 原始层

职责：
- 保存原始事实、导出文档、命令输出、git 状态、测试结果、审计报告、日报原始材料。
- 不做过度总结。
- 不删除、不覆盖原始材料。
- 作为后续所有蒸馏内容的事实依据。

建议目录：
- docs/tabbit/inbox/
- docs/trae-export/inbox/
- docs/reports/daily/
- docs/reports/testing/
- docs/reports/audit/
- docs/reports/coverage/

第二层：Distillation 蒸馏层

职责：
- 从 Raw 层材料中提取可复用的结构化知识。
- 区分事实、判断、风险、规则、后续行动。
- 不把计划写成完成。
- 不把设计写成实现。
- 不把 mock、TODO、空实现写成主链路完成。

建议目录：
- docs/wiki/daily/
- docs/wiki/tasks/
- docs/wiki/decisions/
- docs/wiki/modules/

第三层：Index 目录层

职责：
- 只做导航、摘要、索引、状态入口。
- 不承载完整日报、完整审计、完整对话、完整蒸馏正文。
- LLM-WIKI.md 应成为项目级入口索引，而不是巨型知识正文文件。

建议文件：
- docs/wiki/LLM-WIKI.md
- docs/wiki/CODE-WIKI.md
- docs/wiki/ADR索引.md
- docs/wiki/每日蒸馏索引.md
- docs/wiki/任务索引.md

严格禁止事项：

1. 不允许删除已有日报、蒸馏、审计、任务归档原始材料。
2. 不允许把原始材料直接塞进 LLM-WIKI.md。
3. 不允许把未确认内容写成长期确定规则。
4. 不允许把 SOLO Web 试运行产物写成“已提交”或“最终完成”。
5. 不允许修改业务代码。
6. 不允许修复 Gateway/Tars 代码。
7. 不允许重构 go/python/typescript/proto 业务目录。
8. 不允许执行 destructive 命令。
9. 未经主控确认，不允许 git push。
10. 如果需要 commit，必须先输出提交范围和 diff 摘要，等待主控确认。

本次允许事项：

1. 允许修改 docs/wiki/ 下的 Wiki 结构。
2. 允许新增 docs/wiki/daily/、docs/wiki/tasks/、docs/wiki/decisions/、docs/wiki/modules/ 等目录。
3. 允许新增或更新 docs/wiki/每日蒸馏索引.md。
4. 允许新增或更新 docs/wiki/任务索引.md。
5. 允许重构 docs/wiki/LLM-WIKI.md，使其变成 Index 层入口。
6. 允许新增 LLM Wiki 三层架构说明文档。
7. 允许新增 Trae Skill。
8. 允许新增 Trae Command。
9. 允许更新 docs/wiki/CODE-WIKI.md 中与文档结构、自动化、知识库有关的索引。
10. 允许运行只读检查命令和文档检查命令。

必须先读取的文件：

1. AGENTS.md
2. docs/wiki/LLM-WIKI.md
3. docs/wiki/CODE-WIKI.md
4. docs/wiki/工程规范索引.md
5. docs/wiki/ADR索引.md
6. docs/reports/daily/2026-05-19-SOLO-Web试运行原始记录.md
7. docs/reports/daily/2026-05-19-每日日报.md
8. docs/reports/distilled/2026-05-19-每日知识蒸馏.md
9. docs/reports/daily/2026-05-19-主控汇报.md
10. .trae/skills/ 下已有 Skill 文件
11. .trae/commands/ 下已有 Command 文件

如果某个文件不存在，必须明确写“缺失”，不能跳过。

必须先执行并记录：

```bash
pwd
git remote -v
git branch --show-current
git rev-parse --short HEAD
git status --short
find docs/wiki -maxdepth 3 -type f | sort
find docs/reports -maxdepth 3 -type f | sort
find .trae/skills -maxdepth 2 -type f | sort 2>/dev/null || true
find .trae/commands -maxdepth 2 -type f | sort 2>/dev/null || true
make help
```

如果项目已有 docs/rules/testcase/comment 等检查入口，也请按 Makefile help 中实际存在的 target 执行，不要臆造命令。

本次需要完成的文件改造：

一、重构 docs/wiki/LLM-WIKI.md

目标：

将 LLM-WIKI.md 改造成 Index 层入口文档。

它应该包含：

1. 项目定位
2. 当前阶段
3. 当前最高优先级
4. 后续 AI 必读顺序
5. 三层知识库结构说明
6. Raw 层入口
7. Distillation 层入口
8. Index 层入口
9. 模块索引
10. 决策索引
11. 每日蒸馏索引入口
12. 任务索引入口
13. 当前阻断项
14. 长期工程规则摘要
15. 禁止事项

要求：

- 不要把完整日报、完整审计、完整任务对话放进 LLM-WIKI.md。
- LLM-WIKI.md 只保留摘要和链接。
- 对已确认规则与待确认内容进行明确区分。
- 必须记录：SOLO Web 已验证可生成文件并可遵守不提交限制。
- 必须记录：SOLO Web 不是结构性重构执行者，结构性重构由本地 Trae IDE 完成。
- 必须记录：LLM Wiki 采用 Raw → Distillation → Index 三层结构。

二、新增或更新 docs/wiki/每日蒸馏索引.md

目标：

建立每日蒸馏的 Index 层入口。

建议结构：

```markdown
# 每日蒸馏索引

## 1. 说明

本文是 LLM Wiki 三层结构中的 Index 层文件，用于索引每日 Raw、日报、蒸馏和主控汇报产物。

## 2. 索引表

| 日期 | Raw 原始记录 | 日报 | 蒸馏文件 | 主控汇报 | 状态 |
|---|---|---|---|---|---|
| 2026-05-19 | docs/reports/daily/2026-05-19-SOLO-Web试运行原始记录.md | docs/reports/daily/2026-05-19-每日日报.md | docs/reports/distilled/2026-05-19-每日知识蒸馏.md | docs/reports/daily/2026-05-19-主控汇报.md | 待主控确认 |
```

要求：

- 必须登记 2026-05-19 SOLO Web 试运行产物。
- 状态不得写“已完成提交”，只能写“待主控确认”或“试运行已生成”。
- 如果已有每日蒸馏索引，保留已有内容并增量更新。

三、新增或更新 docs/wiki/任务索引.md

目标：

建立任务级 Index 层入口，索引 Tabbit / Trae / SOLO Web 任务。

建议登记本次任务：

```markdown
| Task ID | 任务名称 | 执行者 | Raw 来源 | 蒸馏文件 | 当前状态 | 主控结论 |
|---|---|---|---|---|---|---|
| TB-20260519-solo-web-daily-trial | SOLO Web 每日产物生成试运行 | SOLO Web | docs/reports/daily/2026-05-19-SOLO-Web试运行原始记录.md | docs/reports/distilled/2026-05-19-每日知识蒸馏.md | 待主控确认 | SOLO Web 可生成文件并遵守不提交限制 |
| TB-20260519-llm-wiki-refactor | LLM Wiki 三层结构重构 | Trae IDE | 本任务执行记录 | 待生成 | 进行中 | 由本地 Trae 完成结构性重构、Skill 与 Command |
```

四、新增 docs/wiki/decisions/llm-wiki-three-layer-architecture.md

目标：

沉淀主控确认的 LLM Wiki 三层架构决策。

必须包含：

1. 背景
2. 为什么不能让 LLM-WIKI.md 继续无限膨胀
3. 三层结构定义
4. Raw 层职责
5. Distillation 层职责
6. Index 层职责
7. SOLO Web 在该结构中的职责
8. Trae IDE 在该结构中的职责
9. Tabbit 主控在该结构中的职责
10. 禁止事项
11. 后续迁移策略

五、新增 docs/wiki/modules/solo-web-automation.md

目标：

记录 SOLO Web 自动化能力边界。

必须包含：

1. 已验证能力
   - 定时触发
   - 调用大模型
   - 生成 Markdown 文件
   - 遵守禁止 commit/push
   - 输出主控汇报
2. 未确认能力
   - 自动提交到 feature 分支
   - 自动创建 PR
   - 跨分支一致性
   - 长期稳定性
3. 推荐职责
   - 每日 Raw 采集
   - 日报生成
   - 蒸馏生成
   - 主控汇报生成
   - Index 更新候选生成
4. 不推荐职责
   - 业务代码修复
   - 架构性重构
   - 自动提交 main
   - 未经确认直接改 LLM-WIKI.md

六、新增 docs/wiki/modules/llm-wiki-architecture.md

目标：

作为模块级知识页，说明 LLM Wiki 的 Raw → Distillation → Index 结构。

七、新增 Trae Skill

请先查看 .trae/skills/ 下已有 Skill 的格式，然后新增一个符合项目格式的 Skill。

建议文件名：

```text
.trae/skills/cairobot-llm-wiki-distillation.md
```

Skill 目标：

用于指导 AI 助手在 CaiRobot MVP 中执行 LLM Wiki 三层蒸馏。

Skill 必须包含：

1. 适用场景
2. 三层结构规则
3. Raw 层处理规则
4. Distillation 层处理规则
5. Index 层处理规则
6. 每日自动化任务规则
7. 禁止事项
8. 输出格式
9. 主控确认机制

必须强调：

- Raw 层保真。
- Distillation 层压缩。
- Index 层导航。
- LLM-WIKI.md 不是正文仓库。
- 未经主控确认，不直接把候选内容写入长期索引。
- 不把计划、设计、mock、TODO 写成已完成。

八、新增 Trae Command

请先查看 .trae/commands/ 下已有 Command 的格式，然后新增一个符合项目格式的 Command。

建议文件名：

```text
.trae/commands/llm-wiki-distill.md
```

Command 目标：

提供一个标准命令入口，用于执行每日或任务级 LLM Wiki 蒸馏。

Command 必须支持以下场景：

1. 每日蒸馏
2. 任务蒸馏
3. SOLO Web 产物归档
4. Tabbit/Trae 导出文档蒸馏
5. Index 更新候选生成

Command 必须要求输出：

1. Raw 来源列表
2. 蒸馏目标
3. 事实摘要
4. 主控判断
5. 风险与限制
6. 后续 AI 规则
7. Index 更新候选
8. 是否需要主控确认

九、可选新增 docs/wiki/README.md

如果项目当前没有 docs/wiki/README.md，可以新增，用于说明 docs/wiki/ 的目录结构。

十、检查与验收

完成后必须运行并汇报：

```bash
git status --short
find docs/wiki -maxdepth 3 -type f | sort
find .trae/skills -maxdepth 2 -type f | sort 2>/dev/null || true
find .trae/commands -maxdepth 2 -type f | sort 2>/dev/null || true
make help
```

如果 Makefile 中存在 docs 检查、规则检查、报告检查等 target，请执行对应 target，并记录结果。

最终汇报格式：

# LLM Wiki 三层结构重构汇报

## 1. 结论

请明确判断：

| 项目 | 结果 | 说明 |
|---|---|---|
| LLM-WIKI.md 是否已改为 Index 层 | pass/fail | |
| Raw 层入口是否明确 | pass/fail | |
| Distillation 层入口是否明确 | pass/fail | |
| Index 层入口是否明确 | pass/fail | |
| 每日蒸馏索引是否建立 | pass/fail | |
| 任务索引是否建立 | pass/fail | |
| LLM Wiki 三层架构文档是否建立 | pass/fail | |
| SOLO Web 能力边界是否记录 | pass/fail | |
| Skill 是否建立 | pass/fail | |
| Command 是否建立 | pass/fail | |
| 是否未提交代码 | pass/fail | |
| 是否未推送代码 | pass/fail | |

## 2. 仓库状态

记录：

- pwd
- remote
- branch
- commit
- git status --short

## 3. 修改文件清单

列出新增和修改文件。

## 4. 三层结构说明

说明 Raw、Distillation、Index 三层的最终落地目录。

## 5. Skill 与 Command 说明

说明新增 Skill 和 Command 的用途、路径和使用方式。

## 6. 检查命令结果

列出执行过的命令和结果。

## 7. 风险与限制

说明是否存在：

- 旧 LLM-WIKI 内容过长
- 旧索引内容重复
- 2026-05-19 产物仍待主控确认
- SOLO Web 后续正式任务 prompt 需要同步更新
- 是否需要下一步提交

## 8. 给主控的建议

请明确建议：

1. 是否接受本次 LLM Wiki 三层结构重构。
2. 是否允许后续 SOLO Web 按此结构生成每日产物。
3. 是否允许创建正式每日 22 点自动化任务。
4. 是否允许提交本次 docs/.trae 变更。
```

---

### **我建议这次 Trae 本地任务的验收口径**

这次不要只看“文件有没有创建”，要看结构是否真正变了。

验收重点是：

| 验收项 | 通过标准 |
|---|---|
| `LLM-WIKI.md` | 变成目录型入口，不再继续堆正文 |
| Raw 层 | 明确指向 `docs/reports/`、`docs/tabbit/inbox/`、`docs/trae-export/inbox/` |
| Distillation 层 | 明确建立 `docs/wiki/daily/`、`docs/wiki/tasks/`、`docs/wiki/decisions/`、`docs/wiki/modules/` |
| Index 层 | 明确建立 `每日蒸馏索引.md`、`任务索引.md` |
| SOLO Web 能力边界 | 明确记录“可生成文件、可拦截提交，但不做结构性重构” |
| Skill | 后续 AI 能按三层结构执行蒸馏 |
| Command | 后续可以通过命令入口触发标准蒸馏流程 |
| 提交控制 | 本次先不推送，等主控看 diff |

### **对 SOLO Web 试运行的主控裁决可以这样写入知识库**

这次最重要的结论不是“生成了 4 个文件”，而是这条：

```text
SOLO Web 已验证具备定时触发、调用大模型、生成 Markdown 文件、遵守不提交/不推送约束的能力。因此，SOLO Web 可承担每日 Raw 采集、日报生成、知识蒸馏、主控汇报和 Index 更新候选生成。但 LLM Wiki 结构性重构、Skill/Command 制作、长期知识库结构调整，应由本地 Trae IDE 执行，并由 Tabbit 主控确认。
```

这条应该进入：

```text
docs/wiki/modules/solo-web-automation.md
docs/wiki/decisions/llm-wiki-three-layer-architecture.md
docs/wiki/每日蒸馏索引.md
docs/wiki/任务索引.md
LLM-WIKI.md 的当前最高优先级或近期主控裁决摘要
```

但在 `LLM-WIKI.md` 里只保留摘要和链接，不要塞完整过程。

### **我这次做了哪些调整**

我把任务拆成了三个层面：先确认 SOLO Web 试运行结论，再把结构性改造交给本地 Trae，最后要求产出 Skill 和 Command。这样做的原因是：SOLO Web 适合定时生成和蒸馏，但不适合作为长期知识库结构的首次重构者；本地 Trae 更适合读取完整仓库、修改 `.trae/skills`、`.trae/commands` 和 `docs/wiki` 结构，并接受主控 diff 审查。

我也把“更新 LLM-WIKI”改成了“重构 LLM-WIKI 为 Index 层入口”。这个修改很关键，因为三层结构里 `LLM-WIKI.md` 不是知识正文池，而是目录层入口。

*内容由 AI 生成仅供参考*