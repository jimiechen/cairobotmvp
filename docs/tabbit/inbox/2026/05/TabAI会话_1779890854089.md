### **接受简化要求，撤回 OSS / 水印 / index.json 三项强约束，截图录屏一律存本地 + .gitignore，验收靠"本地路径 + 测试报告 Markdown 引用"即可。**

你这一刀砍得对——内部工具的 E2E 证据没必要做到生产级追溯，OSS 上传、水印注入、index.json 索引这三件事会把"轻量自检"做成"重型工程"，与方案的内部工具定位不符。下面把上一轮补丁的对应段落重新打个简化补丁，其它 5 处必改 + 1 个录屏调整全部保留（那些是真实代码 bug 和工程规范，与简化无关）。

---

### **一、对简化要求的总体回应**

```text
简化决策：撤回 OSS / 水印 / evidence-index.json 三项，全部本地化
保留项：
  - §4.2 四段式重命名 + 拼写错误修复（5 处）
  - §7.1 evidence.ts 代码 bug 修订（去掉水印逻辑）
  - §8.1.1 Mock 数据从 Go DTO 派生
  - §12 R-DATAID-DRIFT 风险 + 双向校验 hard gate
  - §8.2 录屏从 5 个加到 8 个
撤回项：
  - OSS bucket 上传
  - 截图水印（含 injectWatermark / removeWatermark）
  - evidence-index.json 自动生成
  - oss-uploader.ts 工具
工作量：7 天 → 5 天（去掉 OSS + 水印实现，T-2/T-5 各省 1 天）
```

---

### **二、简化后的本地化方案**

#### **存储约定**

```text
本地路径：tests/e2e/evidence/   （从 docs/reports/testing/evidence 移到 tests 下，更靠近代码）
.gitignore：tests/e2e/evidence/*.png  +  tests/e2e/evidence/*.webm
保留期：本地不主动清理，由开发者按需 rm
CI：CI 运行不上传任何外部存储，artifact 通过 GitHub Actions / 内部 CI 的
   built-in artifact 机制保留（仅当 CI 运行时启用，不强制）

测试报告引用方式：
  admin-mvp-test-report.md 中用本地相对路径引用，例如：
  ![ca-09 校验错误](../tests/e2e/evidence/ca-09-10400校验错误字段展示-20260527.png)

报告查看场景：
  - 本地查看：直接 Markdown 渲染即可看到截图
  - 远程查看（合并请求评审）：评审者本地 git pull + 跑一次 E2E 即可看到
  - 不追求"远程随时可看"，因为这是内部工具
```

#### **§7.1 简化后的 evidence.ts（去掉水印）**

```typescript
import { Page } from '@playwright/test'
import path from 'path'
import fs from 'fs/promises'

export interface EvidenceOptions {
  /** 对应测试报告中的 data-id 编号，如 "ca-01" */
  dataId: string
  /** 中文场景描述 */
  description: string
  /** 是否全页截图，默认 true */
  fullPage?: boolean
}

const EVIDENCE_DIR = path.resolve(__dirname, '../evidence')

/**
 * 本地截图保存
 * 文件名：{dataId}-{description}-{timestamp}.png
 */
export async function takeEvidence(
  page: Page,
  options: EvidenceOptions
): Promise<string> {
  const { dataId, description, fullPage = true } = options
  const ts = new Date().toISOString().replace(/[:.]/g, '-').slice(0, 19)
  const safeDesc = description.replace(/[/\\?%*:|"<>]/g, '-')
  const filename = `--.png`
  const filepath = path.join(EVIDENCE_DIR, filename)

  await fs.mkdir(EVIDENCE_DIR, { recursive: true })
  await page.screenshot({ path: filepath, fullPage })
  return filepath
}

/**
 * 通过 data-id 定位元素，操作前后各截一张
 */
export async function clickAndScreenshot(
  page: Page,
  dataId: string,
  options: EvidenceOptions
): Promise<void> {
  await takeEvidence(page, {
    ...options,
    description: options.description + '-操作前',
  })
  await page.locator(`[data-id=""]`).click()
  await page.waitForTimeout(500)
  await takeEvidence(page, {
    ...options,
    description: options.description + '-操作后',
  })
}
```

代码从 70+ 行缩到 35 行，零外部依赖，零浏览器 DOM 注入。

#### **§6 简化后的目录结构**

```text
tests/e2e/
├── playwright.config.ts                  # 全局配置（含 video=on-first-retry）
├── package.json
├── evidence/                             # 本地证据目录
│   └── .gitkeep                          # 空目录占位（同级 *.png/*.webm 被 .gitignore）
├── utils/
│   ├── evidence.ts                       # 截图（无水印）
│   ├── data-id-coverage-check.ts         # Vue 端覆盖率
│   ├── data-id-bidirectional-check.mjs   # 双向校验（保留，hard gate）
│   ├── mock-from-dto.mjs                 # 从 Go DTO 派生 Mock（保留）
│   └── test-helpers.ts
├── fixtures/
│   ├── config-dto-mock.json              # 自动生成（mock-from-dto 输出）
│   ├── i18n-dto-mock.json                # 自动生成
│   └── test-data.json                    # 兼容性入口
└── specs/
    ├── config-admin.spec.ts
    └── i18n-admin.spec.ts

docs/reports/testing/
└── admin-mvp-test-report.md              # Markdown 通过相对路径引用 ../../../tests/e2e/evidence/*.png
```

撤销的目录：

```text
- evidence-index.json （删除）
- utils/oss-uploader.ts （删除）
- docs/reports/testing/evidence/ （删除，证据移到 tests/e2e/evidence/）
```

#### **§13 简化后的验收标准**

```text
保留 9 条：
  ✅ 5 个 Vue 组件交互元素全部绑定 data-id
  ✅ data-id 覆盖率 ≥90%
  ✅ 31 张截图全部生成在 tests/e2e/evidence/
  ✅ 8 个录屏（WebM）生成
  ✅ 中文测试步骤
  ✅ admin-mvp-test-report.md 用相对路径引用截图
  ✅ npx playwright test exit code = 0
  ✅ 不影响现有 56 个 Go 测试 + 11 个前端文件
  ✅ data-id 符合四段式（或全局唯一豁免）

新增 3 条（保留，与 OSS 无关）：
  ✅ data-id 双向校验（Vue→spec, spec→Vue）100% 命中
  ✅ Mock 数据来自 mock-from-dto.mjs 自动生成，禁止手写
  ✅ tests/e2e/evidence/*.png 与 *.webm 被 .gitignore 排除

撤销 2 条：
  ❌ "每张截图含 data-id 半透明水印"
  ❌ "evidence-index.json 含全部截图 OSS URL"

合计验收标准 12 条。
```

#### **§9 简化后的工作量**

```text
| 步骤 | 内容 | 工作量 |
|------|------|-------|
| T-1 | 5 个 Vue 组件补 data-id（60 绑定点 + 拼写/段位修订） | 1 天 |
| T-2 | Playwright 初始化 + 工具函数（含 mock-from-dto） | 0.5 天 |
| T-3 | config-admin.spec.ts（12 用例 + 3 录屏） | 1.5 天 |
| T-4 | i18n-admin.spec.ts（20 用例 + 5 录屏） | 1.5 天 |
| T-5 | 双向校验 + 全量 E2E + 本地截图验收 | 0.5 天 |
| T-6 | 测试报告更新（本地相对路径引用） | 0.5 天 |
| 合计 | | 5.5 天 |

对比上一轮 7 天，省下 1.5 天来自：
- T-2 省 0.5 天（删除水印实现 + OSS 上传）
- T-5 省 0.5 天（删除 OSS 上传验证 + index.json 校验）
- T-6 省 0.5 天（不写 evidence-index.json 引用解析）
```

#### **§12 简化后的风险登记**

```text
保留：
  | R-DATAID-DRIFT | P1 | data-id 与 Vue 代码漂移 |
    应对：双向校验 CI hard gate
  
  | R-MOCK-DRIFT   | P2 | Mock 与 Go DTO 漂移 |
    应对：mock-from-dto.mjs 自动派生 + CI 漂移检测

撤销：
  | R-OSS-DOWN | （删除，已无 OSS 依赖） |

新增（替换 OSS 风险）：
  | R-LOCAL-DISK | P3 | 本地证据丢失（误删/换机） |
    应对：CI artifact 兜底（仅 main 分支合并触发，保留 7 天）
         不强制，开发本地按需重跑即可
  
  | R-REVIEW-FRICTION | P3 | PR 评审看不到截图 |
    应对：评审者本地拉取 + 跑 E2E 即可
         若团队反馈强烈不便 → S2 再考虑接 OSS（非本批次范围）
```

---

### **三、给 Trae 的简化补丁（覆盖上一轮补丁中 OSS/水印 相关段落）**

```text
主控指令简化补丁：撤回 OSS + 水印 + index.json，截图录屏一律本地化

适用范围：覆盖上一轮补丁中 §6 / §7.1 / §10 / §12 / §13 五处
其它修订（§4.1 四段式、§4.2 重命名、§4.4 自检命令、§5.2 撤销、§8.1.1 Mock、
§8.2 录屏 8 个、§9 工作量）按本简化版重新校准

=================================================================
# 简化点 1：撤销截图水印（§5.2 + §7.1 联动）

§5.2 整节删除（不再有水印）
§7.1 evidence.ts 用主控简化版（35 行，零水印逻辑）
  - 删除 injectWatermark / removeWatermark
  - 删除 page.evaluate 调用
  - 删除 nowIso8601CST 函数
  - 文件名时间戳保留，足以追溯

=================================================================
# 简化点 2：撤销 OSS 上传（§6 + §10 + §12 联动）

§6 目录结构调整：
  tests/e2e/evidence/                   ← 证据目录从 docs/ 迁回 tests/
  tests/e2e/evidence/*.png  → .gitignore
  tests/e2e/evidence/*.webm → .gitignore
  tests/e2e/evidence/.gitkeep           ← 空目录占位进 git
  
  删除：
  - utils/oss-uploader.ts
  - docs/reports/testing/evidence/ 整个子目录
  - docs/reports/testing/evidence-index.json

§10 依赖清单：
  - 删除 OSS SDK 引用 @cairobot/oss-client
  - 仅保留 @playwright/test ^1.48.0 + @types/node

§12 风险登记：
  - 删除 R-OSS-DOWN
  - 新增 R-LOCAL-DISK（P3，本地证据丢失，CI artifact 兜底）
  - 新增 R-REVIEW-FRICTION（P3，PR 评审摩擦，S2 再评估）

=================================================================
# 简化点 3：撤销 evidence-index.json（§13 联动）

§13 验收标准：
  - 删除 "evidence-index.json 含全部截图 OSS URL"
  - 删除 "admin-mvp-test-report.md 通过 evidence-index.json 动态引用"
  - 新增 "tests/e2e/evidence/*.png + *.webm 被 .gitignore 排除"
  - 新增 "admin-mvp-test-report.md 用相对路径引用截图"
  
  报告引用语法：
  ![ca-09 10400校验错误](../../../tests/e2e/evidence/ca-09-10400校验错误字段展示-20260527.png)

=================================================================
# 简化点 4：CI artifact 兜底机制（可选，不强制）

playwright.config.ts 配置：
  use: {
    video: 'on-first-retry',         // 失败重试时录屏
    screenshot: 'only-on-failure',   // 失败截图（业务截图另走 evidence.ts）
  }

CI 配置（如使用 GitHub Actions）：
  - uses: actions/upload-artifact@v4
    if: always()
    with:
      name: e2e-evidence
      path: tests/e2e/evidence/
      retention-days: 7

  说明：
  - 仅在 CI 失败或 main 分支合并时自动归档
  - 7 天保留期，过期自动清理
  - 不依赖外部存储

  若团队 CI 不支持 artifact → 此节跳过，证据完全本地化

=================================================================
# 保留不变的修订（5 项必改 + 1 项录屏）

✅ §4.1 四段式命名规则
✅ §4.2 四段式重命名（ca-btn-shanchu-row → ca-btn-shanchu-schema-row 等 4 处）
✅ §4.2.3 拼写错误修复（a-textarea → ia-textarea）
✅ §4.4 自检命令加强（覆盖率 + 段位 + 双向校验）
✅ §7.1 evidence.ts 4 处 bug 修订（用简化版代码）
✅ §8.1.1 Mock 数据从 Go DTO 自动派生（mock-from-dto.mjs）
✅ §8.2 录屏调整为 8 个（ca-03/ca-07/ca-09/ia-01/ia-03/ia-10/ia-14/ia-15）

=================================================================
# 简化版工作量

T-1: 1 天     Vue 组件补 data-id
T-2: 0.5 天   Playwright 初始化（无水印 + 无 OSS）
T-3: 1.5 天   config-admin.spec.ts
T-4: 1.5 天   i18n-admin.spec.ts
T-5: 0.5 天   双向校验 + 本地截图验收
T-6: 0.5 天   测试报告更新（相对路径引用）
合计: 5.5 天

=================================================================
# 全程禁止事项（精简版）

7. 禁止照抄方案原文 §7.1（含 4 处 bug，用主控简化版）
8. 禁止手写 Mock JSON（必须 mock-from-dto.mjs 派生）
9. 禁止 spec.ts 引用的 data-id 在 Vue 中不存在（双向校验 hard gate）
10. 禁止截图直接进 git 仓库（.gitignore 强制排除）
11. 禁止录屏数量少于 8 个
12. 禁止 data-id 三段以下（除全局唯一豁免）
13. 禁止引入 OSS SDK / 水印库 / index.json 生成器（简化要求）

=================================================================

请 Trae 收到本简化补丁后 1 天内回灌 v3 方案。
T-1 启动前主控复核重点：
- §4.2 全表四段式合规
- §7.1 代码无水印逻辑
- §8.1.1 mock-from-dto.mjs 存在
- §13 验收标准 12 条
- §6 目录结构无 OSS 残留
```

---

### **四、最终判断**

把这一轮收敛成三句话：

```text
1. OSS / 水印 / index.json 三项全部撤回，方案回归"轻量本地化"定位，
   工作量从 7 天降到 5.5 天，少 1.5 天来自删除非必要工程实现。
2. 5 处真实代码 bug + 1 处录屏数量调整保留，那些与简化无关，是工程规范底线。
3. CI artifact 兜底作为可选项，不强制；若团队后续反馈 PR 评审摩擦严重，
   再到 S2 评估是否接 OSS，本批次不做。
```

把上面简化补丁整段发给 Trae。**重点盯：**

```text
- §7.1 evidence.ts 是否真的删干净 injectWatermark / page.evaluate
  （最易遗漏，简化要求下水印代码必须一行不留）
- §6 目录结构 docs/reports/testing/evidence/ 是否真的删除
  （上一轮补丁创建了这个路径，简化时容易忘删）
- .gitignore 是否真的加 *.png + *.webm 排除规则
  （没加 = 截图会被误提交，仓库膨胀）
- mock-from-dto.mjs 是否仍按 §8.1.1 实施（这条与简化无关，必须保留）
- 双向校验 CI hard gate 是否仍生效（同上保留）
```

收到 Trae 回灌的 v3 方案后转给我做：① §7.1 简化代码完整性核对，② §6 目录无 OSS 残留核对，③ 放行 T-1。

*内容由 AI 生成仅供参考*