# 空ModuleKey查询结果 - 测试截图说明

## 测试用例编号
CA-02

## 截图时间
2026-05-28 08:29:39

## 页面描述
Schema 管理页面在 **模块Key（ModuleKey）输入框为空** 时点击查询按钮后的结果展示状态。

## 关键元素

### 1. 查询条件区域
- **模块Key 输入框**：当前为 **空值** 状态，占位符显示"请输入模块标识"
- **查询按钮**：蓝色主按钮，已可点击
- **重置按钮**：白色边框按钮，用于清空查询条件

### 2. 查询结果展示
当 ModuleKey 为空时执行查询操作，系统返回 **全部 Schema 记录**：

| ID | 模块 Key | 字段 Key | 字段类型 | 必填 | 默认值 | 操作 |
|:---:|---------|----------|---------|:---:|--------|------|
| 1 | app.server | port | int (绿色标签) | ✅ | 8080 | 编辑 / 删除 |
| 2 | app.server | timeout | int (绿色标签) | ✅ | 30 | 编辑 / 删除 |
| 3 | app.server | debugMode | bool (红色标签) | ✅ | false | 编辑 / 删除 |
| 4 | cache.redis | host | string (蓝色标签) | ✅ | 127.0.0.1 | 编辑 / 删除 |
| 5 | cache.redis | dblndex | int (绿色标签) | ✅ | 0 | 编辑 / 删除 |

### 3. 数据统计
- **总记录数**：共 5 条
- **当前页码**：第 1 页
- **每页条数**：10 条/页

### 4. 页面其他元素
- **新增 Schema 按钮**：正常显示，可交互
- **批量删除按钮**：正常显示，可交互
- **操作列**：每行记录均显示编辑和删除链接

## 预期行为

1. **空值查询逻辑**：
   - 当 ModuleKey 输入框为空时，系统应将空字符串作为查询条件发送至后端
   - 后端接口应支持空条件查询，返回全部符合条件的记录
   - 不应出现前端报错或后端异常

2. **数据完整性验证**：
   - 查询结果应包含数据库中所有 Schema 记录
   - 各字段数据类型、格式、必填标记应正确渲染
   - 分页信息应与实际记录数一致

3. **UI 状态一致性**：
   - 输入框保持空白状态，不自动填充默认值
   - 表格数据完整加载，无加载中或错误状态提示
   - 所有交互按钮处于可用状态

## 测试结论
✅ **通过** - 空 ModuleKey 查询功能正常，返回完整的 Schema 记录列表。

## 关联测试场景
- 配置管理 > Schema 管理 > 条件查询 > 空值查询
- 前端表单空值处理验证
- 后端接口空参数兼容性测试