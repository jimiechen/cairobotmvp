# Schema列表正常渲染 - 测试截图说明

## 测试用例编号
CA-01

## 截图时间
2026-05-28 08:29:34

## 页面描述
配置管理界面的 **Schema 管理** 列表页面，展示系统已配置的 Schema 记录列表。

## 关键元素

### 1. 页面导航
- **顶部导航栏**：包含"配置管理"和"国际化管理"两个主菜单项
- **面包屑导航**：首页 > Schema 管理（当前页面）

### 2. 查询区域
- **模块Key输入框**：占位符文本为"请输入模块标识"
- **查询按钮**：蓝色主按钮，用于执行查询操作
- **重置按钮**：白色边框按钮，用于清空查询条件

### 3. 操作按钮区
- **新增 Schema 按钮**：蓝色主按钮，用于打开新增 Schema 弹窗
- **批量删除**：红色边框按钮，用于批量删除选中的记录

### 4. 数据表格
表格共显示 **5 条** 配置记录，包含以下列：

| ID | 模块 Key | 字段 Key | 字段类型 | 必填 | 默认值 | 操作 |
|:---:|---------|----------|---------|:---:|--------|------|
| 1 | app.server | port | int (绿色标签) | ✅ | 8080 | 编辑 / 删除 |
| 2 | app.server | timeout | int (绿色标签) | ✅ | 30 | 编辑 / 删除 |
| 3 | app.server | debugMode | bool (红色标签) | ✅ | false | 编辑 / 删除 |
| 4 | cache.redis | host | string (蓝色标签) | ✅ | 127.0.0.1 | 编辑 / 删除 |
| 5 | cache.redis | dblndex | int (绿色标签) | ✅ | 0 | 编辑 / 删除 |

### 5. 分页控件
- **总记录数**：共 5 条
- **每页条数**：10 条/页
- **当前页码**：第 1 页
- **跳转输入框**：前往 [ 1 ] 页

## 预期行为

1. **数据加载**：页面加载时自动请求后端接口获取 Schema 列表数据
2. **表格渲染**：
   - 正确显示所有字段列（ID、模块 Key、字段 Key、字段类型、必填、默认值、操作）
   - 字段类型使用不同颜色标签区分（int=绿色、bool=红色、string=蓝色）
   - 必填字段使用实心圆点图标标识
3. **分页功能**：当数据量超过每页限制时，正确显示分页组件
4. **操作按钮**：每行记录的编辑和删除按钮可点击且样式正确
5. **空状态处理**：ModuleKey 输入框为空时，默认展示全部数据

## 测试结论
✅ **通过** - Schema 列表页面正常渲染，数据显示完整，UI 组件交互正常。

## 关联测试场景
- 配置管理 > Schema 管理 > 列表查询功能
- 前端表格组件渲染验证
- 数据分页功能验证