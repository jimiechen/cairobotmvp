# AGENTS.md

## 1. 项目身份

CaiRobot MVP 是一款家庭教育 AI 学习底座。

产品将手机锁定在透明物理仓内，孩子可以看到手机，但不能触摸或直接操作手机。孩子只能通过受控语音入口提出学习请求。系统必须先进行意图识别、提示词过滤、提示词改写和回答审核，然后才能调用豆包或其他大模型能力。

项目目标：

- 防止手机沉迷。
- 防止儿童直接滥用开放式 AI。
- 支持拍题讲解、错题整理、英语学习和学习计划。
- 保持家长对手机和 AI 使用的控制权。
- 不把产品做成监控孩子的设备。

核心原则：

> 看作业，不看孩子。  
> 管手机，不管人。  
> 引导学习，不替代学习。  
> 先过滤，再调用大模型。  
> 家长拥有最终控制权。

## 2. 全局开发原则

本项目采用 PRD 驱动开发和 TDD 测试驱动开发。

所有开发必须遵守：

1. 没有 PRD，不开功能任务。
2. 没有 Issue，不写代码。
3. 没有测试，不写实现。
4. 测试必须先失败，再实现通过。
5. 不允许擅自扩大功能范围。
6. 不允许把孩子原始输入直接发送给大模型作为最终提示词。
7. 不允许让孩子进入无限制 AI 聊天。
8. 不允许实现直接监控孩子脸部或行为的功能。
9. 不允许在软件异常时自动解锁手机仓。
10. 不允许绕过家长控制。

## 3. 默认语言规范

本项目默认使用中文进行产品定义、需求文档、任务描述、评审、测试说明和开发协作。

除以下情况外，不应使用英文编写项目文档：

- 代码标识符。
- 文件路径。
- JSON 协议字段。
- 命令行。
- 第三方库名。
- 技术标准名。
- Git commit 类型前缀，例如 `feat:`、`test:`、`docs:`。

产品文案、AI 回复模板、测试说明、PRD、Issue、PR 描述必须使用中文。

## 4. 必须遵守的开发流程

每个功能必须按以下流程执行：

1. 阅读相关 PRD。
2. 阅读相关 ADR。
3. 确认 Issue 范围。
4. 编写失败测试。
5. 实现最小代码使测试通过。
6. 小范围重构。
7. 重新运行测试。
8. 更新相关文档。
9. 提交小粒度 Pull Request。

## 5. MVP 范围

MVP 必须包含：

- 透明手机锁仓。
- 手机存在检测。
- 舱门关闭检测。
- 锁状态检测。
- 家长解锁流程。
- 语音学习请求流程。
- 意图分类。
- 提示词过滤。
- 提示词改写。
- 大模型回答审核。
- 拍题讲解流程。
- 基础学习记录。

MVP 暂不包含：

- 开放式娱乐聊天。
- 游戏或短视频入口。
- 儿童监控。
- 人脸追踪。
- 内置投影仪。
- 电池系统。
- 复杂多轴机械臂。
- 无限制外部 App 操作。

## 6. AI 安全规则

孩子输入必须经过以下流程：

1. 输入归一化。
2. 意图识别。
3. 安全过滤。
4. 提示词改写。
5. 调用大模型。
6. 回答审核。
7. 输出儿童友好回复。

孩子原始输入不得直接作为最终提示词发送给模型服务。

## 7. 硬件安全规则

硬件和固件必须遵守故障安全原则：

- App 断连不能导致手机仓自动解锁。
- 过热时必须关闭补光灯并进入保护状态。
- 锁定状态下检测到舱门异常，必须进入错误状态。
- 舱门未关闭时，锁定命令必须失败。
- 解锁必须需要家长授权或机械应急解锁。
- 舵机或电子锁动作必须有超时限制。
- 大电流负载不得直接由 MCU 引脚供电。

## 8. 测试要求

所有模块必须包含正常路径测试和异常路径测试。

AI 安全测试必须覆盖：

- 打开游戏请求。
- 刷视频请求。
- 直接代写作业请求。
- 解锁手机请求。
- 绕过家长控制请求。
- 开放式闲聊请求。
- 提示词注入请求。

固件测试必须覆盖：

- 舱门未关闭。
- 手机未放入。
- 锁定失败。
- App 断连。
- 过热。
- 传感器状态冲突。

## 9. 文档要求

产品需求必须放在：

`docs/prd/`

架构决策必须放在：

`docs/adr/`

设备通信协议必须放在：

`docs/api/`

测试策略必须放在：

`docs/testing/`

任何重大行为变更都必须同步更新相关 PRD 或 ADR。
