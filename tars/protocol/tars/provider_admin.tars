module CaiRobotProviderAdminApp
{
    interface ProviderAdminObj
    {
        int Health(vector<byte> request, map<string,string> extend, out vector<byte> response);
        int HealthCheck(vector<byte> request, map<string,string> extend, out vector<byte> response);

        int CreateProviderAccount(vector<byte> request, map<string,string> extend, out vector<byte> response);
        int ListProviderDevices(vector<byte> request, map<string,string> extend, out vector<byte> response);
        int CreateWorkOrder(vector<byte> request, map<string,string> extend, out vector<byte> response);
        int UpdateWorkOrder(vector<byte> request, map<string,string> extend, out vector<byte> response);
    };
};
